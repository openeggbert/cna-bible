% Shared preamble for "The CNA Bible"
\usepackage{amsmath}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[english]{babel}
\usepackage{geometry}
% Keep the original 40pt header allocation while reserving enough height for
% the handful of deliberately long, two-line running heads.
\geometry{a4paper,margin=2.6cm,headheight=26pt,headsep=14pt}
% Technical prose contains long, indivisible API names. Give TeX a bounded
% last-resort stretch so it can move such a token to the next line instead of
% letting it cross the physical page edge.
\setlength{\emergencystretch}{2.5em}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{parskip}
\usepackage{graphicx}
\usepackage{imakeidx}
\makeindex[intoc]

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=black,
  urlcolor=blue!50!black,
  pdfauthor={Robert Vokac},
  pdftitle={The CNA Bible},
  pdfsubject={A source-grounded guide to CNA and the Microsoft XNA 4.0 programming model},
  pdfkeywords={CNA, Microsoft XNA, C++23, graphics renderers, game development},
  pdflang={en-US}
}

\definecolor{codebg}{RGB}{245,245,245}
\definecolor{codekeyword}{RGB}{0,90,150}
\definecolor{codecomment}{RGB}{120,120,120}
\definecolor{codestring}{RGB}{140,20,20}

\lstdefinestyle{cnacpp}{
  language=C++,
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codekeyword}\bfseries,
  commentstyle=\color{codecomment}\itshape,
  stringstyle=\color{codestring},
  showstringspaces=false,
  breaklines=true,
  frame=single,
  rulecolor=\color{gray!40},
  tabsize=4,
  numbers=left,
  numberstyle=\tiny\color{gray},
  xleftmargin=1.8em,
  framexleftmargin=1.6em
}
\lstset{style=cnacpp}

\lstdefinestyle{cnashell}{
  language=bash,
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  rulecolor=\color{gray!40},
  showstringspaces=false
}

\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titlespacing*{\chapter}{0pt}{0pt}{40pt}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE,RO]{\thepage}
\fancyhead[RE]{\nouppercase{\leftmark}}
\fancyhead[LO]{\nouppercase{\rightmark}}
\renewcommand{\headrulewidth}{0.4pt}

% Cross-reference macros used throughout the book. \cnaclass and \cnans double as the book's
% index markup: every class/method/namespace name they wrap is indexed automatically at every
% occurrence, so the back-of-book index is populated directly from the same markup that already
% exists throughout the text, rather than requiring a second, separate pass of manual \index{}
% placement.
\newcommand{\cnaclass}[1]{\texttt{#1}\index{#1}}
\newcommand{\cnans}[1]{\texttt{#1}\index{#1}}
\newcommand{\repolink}[1]{\texttt{#1}}

% Consistent callout box for "verified fact" style notes drawn from source
\usepackage{mdframed}
\newmdenv[
  backgroundcolor=blue!4,
  linecolor=blue!40!black,
  linewidth=0.6pt,
  roundcorner=2pt,
  skipabove=1em,
  skipbelow=1em,
  innertopmargin=6pt,
  innerbottommargin=6pt,
  innerleftmargin=8pt,
  innerrightmargin=8pt
]{sourcenote}
