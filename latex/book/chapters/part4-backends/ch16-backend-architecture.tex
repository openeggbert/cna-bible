\chapter{Backend Architecture}
\label{ch:backend-architecture}

Every backend chapter that follows implements the same contract. This chapter covers that
contract once --- the interfaces, the shared parameter struct, and the build-time selection
mechanism --- so the nine backend-specific chapters ahead can spend their time on what
actually differs, not on repeating the shape they all share.

\section{One header, not several}

Despite \cnaclass{IGraphicsBackend} being described throughout this book as ``the backend
contract layer,'' it is worth being precise: the entire contract lives in a single header,
\texttt{include/\allowbreak{}CNA/\allowbreak{}Internal/\allowbreak{}Backends/\allowbreak{}Common/\allowbreak{}IGraphicsBackend.hpp} --- roughly a thousand
lines, not a family of separate \texttt{ISpriteBatchBackend.hpp} / \texttt{ITextureBackend.hpp}
files. Eleven interface classes live inside it, all in
\texttt{namespace CNA::Internal::Backends}:

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Interface} & \textbf{Owns} \\
\midrule
\texttt{IVertexBufferBackend} & Vertex data upload, streaming hints, declaration \\
\texttt{IIndexBufferBackend} & 16/32-bit index data upload \\
\texttt{IOcclusionQueryBackend} & Begin/End/IsComplete/PixelCount \\
\texttt{ITextureCubeBackend} & Per-face texture data \\
\texttt{ITexture3DBackend} & Per-slice volume texture data \\
\texttt{ITextureBackend} & 2D texture data, GL interop, CPU shadow \\
\texttt{IRenderTargetBackend} & Extends \texttt{ITextureBackend}: bind/unbind as target \\
\texttt{IRenderTargetCubeBackend} & Extends \texttt{ITextureCubeBackend}: per-face target binding \\
\texttt{IEffectBackend} & Shader program compile/bind, uniform/texture setters \\
\texttt{ISpriteBatchBackend} & Begin/End, transform, three \texttt{Draw} overloads \\
\texttt{IGraphicsBackend} & Everything else --- see below \\
\bottomrule
\end{tabular}
\end{center}

\cnaclass{IGraphicsBackend} itself is the large one: device lifecycle
(\texttt{Clear} / \texttt{Present}/viewport and presentation-mode management), native handle
access (\texttt{GetWindowInternal()} / \texttt{GetRendererInternal()} --- both pure virtual,
and both carry a comment flagging the \texttt{SDL\_Window*} / \texttt{SDL\_Renderer*} return
types as ``should be abstracted later''), resource factories for every GPU object type in
Part~III, render-target binding, per-state-object apply methods, the full 3D draw dispatch
(\texttt{DrawColoredPrimitives}, the effect-aware \texttt{DrawPrimitivesEx} family, instanced
draws), and a capability-query mechanism (\S\ref{sec:graphicscapability} below). A single,
project-wide design choice worth internalizing before reading any backend chapter: nearly
every optional method on this interface \textbf{defaults to a safe no-op or a null return}
rather than requiring every backend to implement everything. \texttt{CreateEffectBackend()}
defaults to \texttt{nullptr} (``backends that do not support programmable shaders'');
\texttt{CreateOcclusionQuery()} defaults to \texttt{nullptr}; every state-application method
defaults to doing nothing at all. This is a deliberate ``graceful capability degradation''
design, and every backend chapter's own ``degrades/gap/blocked'' distinctions in Part~IV rest on it: a feature
that silently degrades to a no-op reads differently in an audit than one that throws, even
though both mean ``not implemented here.''

\subsection{ISpriteBatchBackend, and where Chapter~\ref{ch:spritebatch}'s transform bug actually lived}

Only five of \cnaclass{ISpriteBatchBackend}'s nine methods are pure virtual ---
\texttt{Begin()}, \texttt{End()}, and the three \texttt{Draw} overloads
(\texttt{float x, float y}; destination-plus-source-rectangle; and a fully-parameterized
rotation/origin/effects/depth form). \texttt{SetTransformMatrix}, \texttt{SetCustomEffect},
\texttt{SetSamplerFilter}, and \texttt{SetSamplerAddressMode} all default to a plain, silent
no-op body in the interface itself. This is the exact mechanism behind
Chapter~\ref{ch:spritebatch}'s own worst SDL\_RENDERER finding: a backend that simply never
overrides \texttt{SetTransformMatrix} is not violating the interface contract in any way a
compiler could catch --- it is using the interface exactly as designed, ``graceful capability
degradation'' silently reached its most literal form, a \texttt{Begin(transformMatrix)} call
whose matrix argument reaches the backend and is discarded with zero indication anything was
skipped. Reading this interface's own defaults is why that bug reads as an architecturally
expected failure mode, not a surprising one, and it also names a real, genuine gap in the
capability-query mechanism covered next: \cnaclass{GraphicsCapability} (below) has no value at
all for ``does this backend actually honor a \cnaclass{SpriteBatch} transform matrix,'' so a
caller has no way to \emph{ask} before hitting this specific silent no-op --- unlike the eight
capabilities that mechanism does cover.

\subsection{Window/logical input transforms: three dispatch tiers}
\label{sec:backend-input-coordinate-matrix}

\cnaclass{IGraphicsBackend}'s two coordinate hooks both default to returning
\texttt{false}. The read hook is
\texttt{TransformWindow\allowbreak ToLogical()}; its inverse is
\texttt{TransformLogical\allowbreak ToWindow()}. That sounds like one ordinary
unsupported-feature sentinel, but the public callers place two routes ahead of it:

\begin{enumerate}
\item if the window has an associated SDL renderer, mouse and touch use SDL's renderer
  coordinate-conversion API;
\item otherwise they consult \cnaclass{IGraphicsBackend}'s static window-to-backend registry
  and call the corresponding virtual hook;
\item if neither route succeeds, the original coordinates pass through unchanged.
\end{enumerate}

The read direction is shared by absolute mouse motion, mouse-button positions, and touch
coordinates. The inverse is used by \cnaclass{Mouse::SetPosition()} before the OS cursor is
warped. This makes both the override matrix and the dispatch tier load-bearing: an excellent
transform that the backend never registers may still be dead, while a backend with no
override may be correct because an SDL renderer wins first.

\begingroup
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.14\textwidth}p{0.24\textwidth}p{0.31\textwidth}p{0.23\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Public route} & \textbf{Actual coordinate contract} &
  \textbf{Located evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Public route} & \textbf{Actual coordinate contract} &
  \textbf{Located evidence} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak RENDERER}, \texttt{ASCII}
  & Associated SDL renderer, not a backend override.
  & SDL implements all five presentation modes, including scale and letterbox offset.
  & Public read-side event test and direct write-side conversion tests. ASCII's own immediate
    set/get check is weaker for the reason below. \\
\texttt{EASYGL}
  & Registered backend override.
  & Offset-free height scaling is exact for \texttt{FixedHeightDynamicWidth}; the other stored
    modes do not have matching transform/output implementations.
  & Source only; no dedicated coordinate test was found. \\
\texttt{CANVAS}
  & Registered backend override.
  & Same height-only contract as EasyGL.
  & Its unit test explicitly excludes the window-dependent transform methods. \\
\texttt{WEBGPU}, \texttt{SDL\_GPU}
  & Registered backend override.
  & Shared five-mode math uses physical pixels, while SDL events and cursor warps use window
    coordinates; high pixel density therefore introduces a scale error. A letterbox-bar read
    also returns \texttt{false}, making the caller substitute raw window coordinates.
  & Source only; no dedicated coordinate test was found. \\
\texttt{DX3}
  & The SDL renderer created inside \repolink{free-direct} wins before DX3's overrides.
  & Hardcoded letterbox mapping becomes active lazily on the first \texttt{Present()}; before
    that, public input passes through. The direct overrides are shadowed during normal use.
  & Four-property test proves the shadowed helper math, not public input routing. \\
\texttt{VULKAN}
  & Inherited \texttt{false}; raw pass-through.
  & SpriteBatch projects the virtual size across the physical swapchain, so a resized or
    otherwise non-1:1 window makes absolute input disagree with rendered coordinates.
  & Source only; no dedicated coordinate test was found. \\
\texttt{BGFX}
  & Inherited \texttt{false}; raw pass-through.
  & The backend explicitly ignores virtual resolution and uses a physical viewport, so
    pass-through agrees with that separate presentation limitation.
  & Source only. \\
\texttt{D3D9}, \texttt{D3D11}, \texttt{D3D12}
  & Inherited \texttt{false}; raw pass-through.
  & Presentation-mode scaling is stored or a no-op and rendering remains physical, so the
    fallback is internally consistent but does not implement the requested presentation mode.
  & Source only; no dedicated input-coordinate test was found. \\
\texttt{SOFTWARE}, \texttt{HEADLESS}
  & Inherited \texttt{false}; no normal window route exists.
  & Logical pixels are internal buffers only; there is no OS cursor or SDL window to map.
  & Source-proven by both null window accessors. \\
\bottomrule
\end{longtable}
\endgroup

The WebGPU/SDL GPU density distinction is not inferred from names alone. SDL's own
\texttt{SDL\_video.h} says \texttt{SDL\_GetWindow\allowbreak Size()} returns
window-coordinate size and
\texttt{SDL\_GetWindow\allowbreak SizeInPixels()} returns the drawable pixel size; its
renderer conversion API explicitly accepts and returns window coordinates. Both backends
build \texttt{ComputeLogical\allowbreak Viewport()} from the latter physical size, then feed
the result directly to/from mouse-event and warp coordinates from the former space.

Two test boundaries prevent false confidence. \cnaclass{Mouse::SetPosition()} first writes the
requested logical point straight into \texttt{InputManager}, then attempts the physical warp.
An immediate \texttt{SetPosition}/\texttt{GetState} round trip therefore passes even if
coordinate conversion and the warp are both wrong; the ASCII integration test has exactly
this shape. The DX3 test instead calls both backend methods directly, bypassing the
SDL-renderer-first dispatch which shadows them. The strongest existing public evidence is the
SDL read-side test: it injects a physical motion event into a $200\times200$ window with a
$100\times100$ logical presentation and observes logical $(50,50)$.

FNA avoids a backend registry entirely. Its \cnaclass{Mouse::GetState()} multiplies each
window coordinate by the independent backbuffer/window dimension ratio; \texttt{SetPosition}
uses the inverse ratios. CNA's richer five-mode presentation model needs offsets and
mode-specific viewport math that FNA's simple faux-backbuffer scale does not represent, but
the table shows that this additional mechanism is not yet uniform or uniformly tested.

\subsection{\texttt{SetSwapInterval}: exact public enum, four backend meanings}
\label{sec:backend-swap-interval-matrix}

The default swap-interval body is empty. Its comment defines
\texttt{0=Immediate}, \texttt{1=VSync}, and \texttt{2=half-rate}, but neither the type system
nor \cnaclass{GraphicsCapability} can tell a caller whether a backend implements that
contract. The public conversion itself loses nothing:
\texttt{Immediate} becomes~0, \texttt{Two} becomes~2, and
\texttt{Default}/\texttt{One} become~1. Both backend construction and
\cnaclass{GraphicsDevice}\texttt{::Reset()} receive the result. The reset forwarding is a
real repaired path; older CNA builds updated the stored
\cnaclass{PresentationParameters} but never called this virtual from
\texttt{GraphicsDevice\allowbreak Manager::ApplyChanges()}.

Two lifecycle details are necessary to read the matrix correctly. First, the device
manager's vertical-retrace preference is a Boolean and therefore selects only
\texttt{One} or \texttt{Immediate}; \texttt{Two} requires explicit
\cnaclass{PresentationParameters}, commonly through
\texttt{Preparing\allowbreak DeviceSettings}.
Second, CNA's \cnaclass{Game} eagerly constructs its \cnaclass{GraphicsDevice} and backend
with default parameters before the derived game can configure its device manager. A backend
that consumes the construction argument but inherits the runtime no-op therefore does
\emph{not} honor the ordinary game-level preference.

\begingroup
\small
\setlength{\tabcolsep}{3.0pt}
\renewcommand{\arraystretch}{1.13}
\begin{longtable}{@{}p{0.13\textwidth}p{0.20\textwidth}p{0.29\textwidth}p{0.30\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Construction} & \textbf{Reset/runtime and \texttt{Two}} &
  \textbf{Located evidence and failure boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Construction} & \textbf{Reset/runtime and \texttt{Two}} &
  \textbf{Located evidence and failure boundary} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak RENDERER}, \texttt{ASCII}
  & Their shared SDL renderer converts every positive initial value to~1.
  & Runtime passes the exact integer to \texttt{SDL\_SetRenderVSync}; if a value above~1 is
    rejected it tries~1. Thus \texttt{Two} is attempted only after construction.
  & Public test proves reset routing, parameter round-trip, and continued pixels, not actual
    renderer state. A raw local probe rejected~2. ASCII has no separate interval test. \\
\texttt{EASYGL}
  & Stores and submits the exact integer through
    \texttt{SDL\_GL\_SetSwap\allowbreak Interval}.
  & Repeats the exact call, including~2, but ignores its success result and has no fallback.
  & Public manager/reset test queries the real GL context and proves 0 versus nonzero.
    A second test only proves that all enum values round-trip without a crash; no test proves~2. \\
\texttt{VULKAN}
  & 0 prefers Immediate then Mailbox; 1 uses FIFO; 2 prefers FIFO Relaxed, then FIFO.
  & Inherits the no-op. FIFO Relaxed is adaptive late tearing, not half-refresh pacing.
  & Source only. In an ordinary \cnaclass{Game}, the eager default construction fixes FIFO;
    later manager/reset changes, including disabling VSync, do not alter the present mode. \\
\texttt{BGFX}
  & Any positive value sets \texttt{BGFX\_RESET\_VSYNC}.
  & Repeats that Boolean mapping and calls \texttt{bgfx::reset}; \texttt{Two} equals
    \texttt{One}.
  & Source only; no dedicated interval or timing assertion was found. \\
\texttt{WEBGPU}
  & 0 prefers Immediate then Mailbox; positive values select FIFO.
  & Stores a nonnegative value and reconfigures the surface when it changes; \texttt{Two}
    still selects FIFO.
  & Source only. Unsupported immediate modes fall back to the initial FIFO choice; no
    half-rate WebGPU present mode is requested. \\
\texttt{SDL\_GPU}
  & Calls the same runtime handler: 0 prefers Immediate then Mailbox, positive means VSync.
  & Reconfigures swapchain parameters; \texttt{Two} equals \texttt{One}. Failure is only
    logged while the requested integer remains stored.
  & Nineteen tests had to use the public manager property after real VSync caused roughly
    one-second virtual-display frames. This proves observable on/off routing, not
    half-refresh behavior. \\
\texttt{D3D9}
  & Maps 0/1/2 to the matching native D3D9 presentation interval.
  & A change immediately resets the device. \texttt{Two} is literal, but CNA neither checks
    the driver capability bit nor restricts it to fullscreen.
  & Source plus the official D3D9 contract: windowed mode permits only Default, Immediate,
    and One. A rejected reset is logged and retried every present; no dedicated interval
    test or real-Windows proof was found. \\
\texttt{D3D11}, \texttt{D3D12}
  & Store only \texttt{args.swapInterval > 0}.
  & Runtime stores the same Boolean; \texttt{Present()} passes DXGI sync interval 1 or~0.
    \texttt{Two} therefore collapses despite DXGI accepting values 1--4.
  & Real windowed present smoke tests exercise only the default VSync branch. No dedicated
    on/off, tearing, or interval-2 assertion was found; D3D12 headless mode cannot present. \\
\texttt{CANVAS}
  & Factory discards the argument.
  & Inherits the no-op; browser composition paints the canvas automatically.
  & Source only. \texttt{Present()} itself is empty, so no swap interval exists at this
    abstraction layer. \\
\texttt{DX3}
  & Constructor ignores the argument.
  & Inherits the no-op. Present is a waiting DirectDraw \texttt{Blt} into free-direct's
    primary surface, with no interval selection.
  & Source only; no timing or interval test was found. \\
\texttt{SOFTWARE}
  & Factory discards the argument.
  & Inherits the no-op and has an empty \texttt{Present()}.
  & Source-proven non-applicability: there is no window or display pacing. \\
\texttt{HEADLESS}
  & Factory discards the argument.
  & Inherits the no-op. \texttt{Present()} advances trace counters, never time.
  & Source-proven diagnostic behavior; no window or retrace exists. \\
\bottomrule
\end{longtable}
\endgroup

The D3D rows make the difference between an API limitation and a CNA narrowing especially
clear. DXGI's own \texttt{Present(SyncInterval,\ldots)} accepts 1 through~4, so D3D11/12
could represent \texttt{Two}; CNA throws the distinction away in its Boolean
\texttt{vsyncEnabled\_}. D3D9 preserves the integer but omits the native validity checks,
which can be worse than a clean downgrade: a normal windowed game can retain
\texttt{PresentInterval::Two} in its public property while the device repeatedly keeps its
previous presentation configuration.

Test names also need careful interpretation. The SDL Renderer regression cycles all four
values and reads a pixel afterward, but the backend exposes no public actual-VSync query;
that assertion proves a safe route, not the driver's accepted interval. EasyGL's stronger
manager test calls \texttt{SDL\_GL\_GetSwapInterval()} and therefore proves real off/on
state, but still never requests~2. No located timing test distinguishes half refresh from
ordinary VSync on any product. FNA's shared path instead sends the complete
\cnaclass{PresentationParameters} to \texttt{FNA3D\_ResetBackbuffer}; it does not expose
CNA's per-backend empty virtual at all.

\subsection{\texttt{UpdatePresentationFormatEXT}: stored fields, window state, and native attachments}
\label{sec:backend-presentation-format-matrix}

Three fields travel together through this \texttt{NOXNA} backend hook:
\cnaclass{SurfaceFormat} for the back buffer, \cnaclass{DepthFormat} for its depth/stencil
attachment, and \texttt{IsFullScreen}. They must be audited separately. On every
\cnaclass{GraphicsDevice}\texttt{::Reset()}, CNA first copies the whole requested object into
its public \cnaclass{PresentationParameters}. It then calls
\texttt{SDL\_SetWindowFullscreen()} and, outside Android,
\texttt{SDL\_SetWindowSize()}. Fullscreen failure is cleared and ignored; size failure throws.
Only after that window work does the device reapply virtual resolution, MSAA, and swap
interval, then call \texttt{UpdatePresentationFormatEXT()}.

Only D3D9 overrides the empty interface body. This matters during ordinary \cnaclass{Game}
construction. A bare presentation-parameter object initially means
\texttt{Color}/\texttt{None}/windowed, so the game eagerly creates its backend with those
values. The subsequently-created manager defaults instead to
\texttt{Color}/\texttt{Depth24}/windowed and delivers that combination through
\texttt{ApplyChanges()} and \texttt{Reset()}. D3D9 needed the runtime hook so its initial
no-depth device became the requested Depth24 device. Every other product either never
consults the three constructor fields or keeps its independently-chosen attachments.

\begingroup
\small
\setlength{\tabcolsep}{2.8pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{@{}p{0.13\textwidth}p{0.25\textwidth}p{0.26\textwidth}p{0.28\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Native color target} & \textbf{Native depth/stencil} &
  \textbf{Fullscreen/reset behavior and evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Native color target} & \textbf{Native depth/stencil} &
  \textbf{Fullscreen/reset behavior and evidence} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak RENDERER}, \texttt{ASCII}
  & SDL chooses the window renderer's output format. CNA does not pass
    \texttt{BackBufferFormat}; its owned textures are fixed RGBA32.
  & None. Both are 2D-only; the renderer target's
    \texttt{HasRealDepthBuffer()} is always false.
  & The shared SDL window call is the only fullscreen mechanism. Tests prove parameter
    round-trip/no throw and post-toggle pixels under Xvfb, explicitly not that the display
    accepted fullscreen. ASCII wraps the same renderer. \\
\texttt{EASYGL}
  & The default framebuffer is chosen by SDL/GL. CNA requests no PP-derived color sizes.
    With MSAA it instead renders through a fixed RGBA8 renderbuffer.
  & The context request asks for 8 stencil bits but does not derive attachment sizes from
    \cnaclass{DepthFormat}. The MSAA FBO is fixed Depth24 with no stencil attachment.
  & SDL handles the window. Dedicated depth/fullscreen examples assert stored fields and
    no-throw behavior only; their comments explicitly disclaim native reconfiguration. \\
\texttt{VULKAN}
  & Prefers \texttt{B8G8R8A8\_UNORM} with the ordinary nonlinear display color space, else
    takes the first surface format. The requested \cnaclass{SurfaceFormat} is never read.
  & Always allocates one device-selected backbuffer depth image, preferring D24S8, then
    D32S8, then D32. Even \texttt{None} does not remove it.
  & SDL changes the window; normal out-of-date/resize handling rebuilds the swapchain.
    Format/depth conclusions are source-only. Render-target depth fidelity is separate. \\
\texttt{BGFX}
  & bgfx owns the implicit platform backbuffer; CNA supplies dimensions and reset flags, not
    the requested pixel format.
  & The platform backbuffer's depth/stencil allocation is likewise bgfx-owned. CNA submits
    depth/stencil clears but neither selects nor queries its exact format.
  & SDL changes the window; the next size check calls \texttt{bgfx::reset}. No dedicated
    format/fullscreen assertion was found. \\
\texttt{WEBGPU}
  & Chooses from surface capabilities, preferring BGRA8-sRGB, then RGBA8-sRGB, then their
    UNORM variants. Thus public \texttt{Color} can accompany a real sRGB swapchain.
  & Always creates \texttt{Depth24PlusStencil8}, regardless of
    \texttt{DepthStencilFormat}, and recreates it with the physical surface size.
  & SDL changes the window; the next frame detects physical-size changes and reconfigures the
    surface. Existing depth tests prove the fixed attachment works, not format selection. \\
\texttt{SDL\_GPU}
  & Reads the swapchain texture format selected by SDL\_gpu on every frame; the PP color
    format never participates.
  & At construction, chooses D24S8 or D32S8 by device support and allocates it whenever a
    frame needs the backbuffer. It is absent only if neither combined format is supported,
    not because the caller requested \texttt{None}.
  & SDL owns the window and swapchain acquisition follows it. No discriminating default
    attachment/fullscreen test was found. \\
\texttt{D3D9}
  & Maps the requested enum to \texttt{D3DFORMAT}. \texttt{Color}'s texture-native
    A8B8G8R8 is specially substituted with display-compatible A8R8G8B8; unknown values fall
    back to that color format.
  & Exact None/D16/D24X8/D24S8 mapping. A runtime change immediately performs a real device
    \texttt{Reset()}.
  & The same native parameters set \texttt{Windowed = !IsFullScreen}. Mapping tests and the
    smoke test prove D24S8 exists after the manager reset. Unsupported combinations can still
    fail while public requested state remains changed. \\
\texttt{D3D11}
  & Always R8G8B8A8 UNORM.
  & Always D24S8, including a public \texttt{None} or Depth24 request.
  & SDL may change the containing window, but the flip-model swapchain is always created
    windowed; CNA never calls \texttt{SetFullscreenState}. Native formats are source-proven. \\
\texttt{D3D12}
  & A windowed swapchain is fixed R8G8B8A8 UNORM. In \texttt{HeadlessEXT} mode there is no
    swapchain/backbuffer at all.
  & A windowed swapchain gets fixed D24S8. Headless mode has no default attachment; only
    explicitly-created render targets can have one.
  & Same SDL-window-only fullscreen semantics as D3D11; no DXGI exclusive transition.
    Ordinary Linux/Wine tests predominantly exercise the off-screen path. \\
\texttt{CANVAS}
  & The browser's Canvas2D main canvas is the color target; the XNA surface format is ignored.
  & None. \texttt{SupportsDepthStencil()} and every target's
    \texttt{HasRealDepthBuffer()} return false.
  & CNA still tries the SDL fullscreen call, but no real browser/DOM acceptance assertion was
    found. The public PP can change independently. \\
\texttt{DX3}
  & Always creates a 32-bpp DirectDraw primary plus a 32-bpp shadow backbuffer; no
    \cnaclass{SurfaceFormat} is read.
  & None; all 3D/depth methods throw.
  & SDL changes the window, while free-direct remains in
    \texttt{DDSCL\_NORMAL}; CNA never requests an exclusive DirectDraw cooperative level.
    Existing resize/pixel tests do not discriminate fullscreen. \\
\texttt{SOFTWARE}
  & Always a CPU RGBA8 vector.
  & Always a float depth vector; stencil operations are no-ops. The PP depth enum cannot
    add or remove either.
  & No SDL window and empty \texttt{Present()}; fullscreen is stored state only. Source and
    pixel tests prove the fixed CPU storage. \\
\texttt{HEADLESS}
  & No pixel attachment; readback synthesizes the last clear color.
  & No attachment; depth/stencil calls only validate/record trace state.
  & No window. All three fields are diagnostic stored state with no presentation object to
    change. \\
\bottomrule
\end{longtable}
\endgroup

The most counterintuitive row is not necessarily the least capable one. Software, Vulkan,
WebGPU, SDL GPU, and D3D11/12 can perform real depth testing while ignoring the requested
depth format; their problem is \emph{selection fidelity}. Asking for \texttt{None} does not
save the allocation, and asking for Depth16 does not create 16-bit depth. Conversely SDL
Renderer, ASCII, Canvas, and DX3 honestly have no default depth attachment at all, regardless
of the stored manager default.

Fullscreen also cannot be collapsed into a Boolean capability mark. SDL can change an
ordinary window before the backend hook, and resize-sensitive swapchains can follow its new
dimensions without consuming \texttt{isFullScreen}. That produces borderless/window-manager
fullscreen on many rows, not necessarily the exclusive native mode implied by D3D9's
\texttt{Windowed} field. Because CNA swallows an SDL fullscreen failure, the public property
never becomes an actual-state query on any row.

The located tests reinforce that boundary rather than closing it. The EasyGL depth-format
test deliberately cycles all four enums and checks only the stored PP; the EasyGL and SDL
Renderer fullscreen tests state that virtual displays may reject the transition and assert
round-trip/no-throw behavior. D3D9 is the exception with a native
\texttt{D3DFMT\_D24S8} surface assertion after manager-driven setup. No dedicated test was
found that requests two distinct \cnaclass{SurfaceFormat} backbuffers and queries the native
result. FNA passes the complete native presentation structure to
\texttt{FNA3D\_ResetBackbuffer}; CNA's empty per-field hook is its own architecture, not an
FNA behavior to infer from a successful public reset.

\subsection{\texttt{ReadBackbuffer}: one name, four observable contracts}
\label{sec:backend-readback-matrix}

\cnaclass{IGraphicsBackend}'s comment promises RGBA8 pixels, top-left coordinates in game
space, and \(w\times h\times4\) bytes of output. Its implementation does not silently
degrade: it throws
\texttt{runtime\_error("ReadBackbuffer: not implemented in this backend")}. Twelve products
override it; \texttt{SDL\_GPU} and \texttt{D3D12} do not.

The public layer does not make those twelve overrides equivalent.
\texttt{GetBackBuffer\allowbreak Data()} passes a supplied rectangle through unchanged; for a
null rectangle it asks \texttt{GetViewport\allowbreak Size()} for \(w,h\). It rejects a null output
pointer and \texttt{elementCount < w*h}, then allocates an RGBA byte buffer, calls the virtual,
and writes exactly \(w\times h\) \cnaclass{Color} objects starting at \texttt{startIndex}.
It does not validate a negative offset, the allocation capacity after that offset, rectangle
bounds, or signed multiplication overflow. Those wrapper gaps are shared; the table below
records the separate backend behavior reached after them.

\begingroup
\small
\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{1.13}
\begin{longtable}{@{}p{0.13\textwidth}p{0.25\textwidth}p{0.31\textwidth}p{0.23\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Image actually read} & \textbf{Region and special behavior} &
  \textbf{Located evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Image actually read} & \textbf{Region and special behavior} &
  \textbf{Located evidence} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak RENDERER}
  & The current SDL render target; the window only when no target is bound.
  & Converts to packed RGBA and honors surface pitch. Window readback throws when logical and
    physical presentation sizes differ; a 1:1 image may still use the presentation offset.
  & Public exact-pixel tests select \texttt{NativeBackBuffer}; a dedicated target test proves
    the bound-target route. \\
\texttt{ASCII}
  & Its internal logical \texttt{gameTarget\_}, or an explicit user target. Never the final
    visible glyph grid.
  & Forwards to SDL Renderer. The displayed quantized grid has a separate test-only
    real-backbuffer helper used before swap.
  & Off-screen test distinguishes the game target, explicit target, and public route; present
    test uses the private final-grid helper. \\
\texttt{SOFTWARE}
  & \texttt{CurrentFramebuffer()}: bound target or CPU backbuffer.
  & Exact top-left CPU copy; out-of-range source pixels become transparent black.
  & Smoke test reads an exact clear, then distinguishes a still-bound target from the
    restored backbuffer. \\
\texttt{HEADLESS}
  & No rendered image: a synthetic fill of the last global \texttt{Clear} color.
  & Ignores \(x,y\) and all draw output; only rejects negative dimensions.
  & Source only for this public result; existing smoke checks trace/resource state, not
    returned pixels. \\
\texttt{EASYGL}
  & The currently bound GL framebuffer.
  & Flips GL's Y origin and rows; resolves default-framebuffer MSAA. For the window,
    \texttt{GetViewportSize()} reports logical dimensions but \texttt{glReadPixels} addresses
    physical framebuffer pixels, so non-1:1 presentation can scale or offset the region.
  & Many public pixel tests; target-usage test explicitly proves bound-FBO readback. Existing
    cases are predominantly 1:1. \\
\texttt{DX3}
  & \texttt{ActiveSurface()}: bound target or its logical shadow backbuffer.
  & Exact logical pixels before free-direct's later physical presentation scaling.
  & Dedicated texture/target test reads the bound target and then the restored shadow
    backbuffer. \\
\texttt{VULKAN}
  & Always the physical swapchain, even while a render target is bound.
  & Captures the full image into a staging cache before deferred presentation, swizzles the
    swapchain format, and zero-fills out-of-range pixels.
  & Broad public pixel coverage; the target round-trip test explicitly documents and proves
    that a bound target is \emph{not} the source. \\
\texttt{BGFX}
  & Always the default swapchain via
    \texttt{requestScreenShot(\allowbreak BGFX\_INVALID\_HANDLE)}.
  & Honors callback pitch, Y direction, and BGRA/RGBA order, but performs no source-rectangle
    bounds check. Each read may advance as many as three bgfx frames; window-coordinate size
    and screenshot pixel size can diverge on high-density displays.
  & Public pixel tests compensate by rerendering/retrying; that proves pixels but also exposes
    the multiple-read-per-frame caveat. \\
\texttt{CANVAS}
  & Under Emscripten, the current Canvas2D context: bound target or main canvas.
  & Synchronous \texttt{getImageData}. The non-Emscripten body is a silent no-op, leaving the
    wrapper's zero-initialized output unchanged. Logical viewport dimensions can differ from
    canvas backing-store pixels.
  & Source only; no real browser/DOM public readback execution was found. \\
\texttt{D3D11}
  & Always \texttt{backBufferTexture\_}, independent of render-target binding.
  & Copies to a fresh staging texture, honors mapped row pitch, and returns top-left RGBA.
  & Real windowed smoke tests discriminate two clear colors and regions. Their small regions
    do not independently force a padded-row case. \\
\texttt{WEBGPU}
  & Always the physical surface image, independent of render-target binding.
  & On-demand render, 256-byte-aligned staging rows, format swizzle, and transparent-black
    out-of-range fill. The wrapper obtains logical dimensions but the copy is indexed in
    physical pixels, so scaled/high-density regions are mismatched.
  & Public clear/readback and mutation/revert pixel proofs; no non-1:1 coordinate case. \\
\texttt{SDL\_GPU}
  & None: inherited default throws.
  & The acquired swapchain texture is write-only in SDL's API. A readable proxy would add a
    texture and blit to every frame and is deliberately not implemented.
  & Render-target \texttt{GetData} is tested separately; it does not make public backbuffer
    readback available. \\
\texttt{D3D9}
  & Always the actual Direct3D backbuffer.
  & Copies to system memory, honors pitch, handles the two supported RGBA/BGRA layouts, and
    rejects other formats or a lost device.
  & Real smoke/draw tests assert exact returned pixels. \\
\texttt{D3D12}
  & None: inherited default throws, including on a windowed device.
  & Its \texttt{HeadlessEXT} mode has no swapchain at all. The backend owns working
    render-target resource readback, but no \texttt{ReadBackbuffer} bridge.
  & Headless smoke tests deliberately read target resources directly; they do not exercise
    \cnaclass{GetBackBufferData}. \\
\bottomrule
\end{longtable}
\endgroup

The bound-target split is therefore exact, not merely a testing quirk.
\texttt{SDL\_RENDERER}, \texttt{ASCII}, \texttt{SOFTWARE}, \texttt{EASYGL}, \texttt{DX3},
and \texttt{CANVAS} follow the active target; \texttt{VULKAN}, \texttt{BGFX},
\texttt{D3D11}, \texttt{WEBGPU}, and \texttt{D3D9} stay on the real backbuffer;
\texttt{HEADLESS} is synthetic; two products throw. FNA's public generic overload instead
queries the actual backbuffer size and surface format and calls
\texttt{FNA3D\_\allowbreak ReadBackbuffer}, without a current-render-target branch. CNA has
no \texttt{Graphics\allowbreak Capability} value for this distinction, so portable caller code cannot
query it through the capability API; it must treat readback support, source image, and
coordinate space as an explicitly selected backend contract.

\subsection{IRenderTargetBackend: where Chapter~\ref{ch:textures-rendertargets}'s honest defaults actually live}

Reading \cnaclass{IRenderTargetBackend}'s own interface definition directly explains
\emph{why} several of Chapter~\ref{ch:textures-rendertargets}'s per-backend render-target
findings take the specific shape they do, rather than being independent per-backend choices:
\texttt{GetMultiSampleCount()} defaults to returning \texttt{0}, and
\texttt{HasRealDepthBuffer(bool depthFormatWasRequested)} defaults to simply echoing back
whatever was requested. Vulkan and BGFX ``honestly reporting \texttt{MultiSampleCount = 0}''
is not a special case either backend had to write --- it is what happens automatically by
\emph{not overriding} this method at all, exactly the same ``graceful capability degradation''
default-no-op pattern this chapter opened with, applied here to a getter rather than a
command. \textbf{SDL\_RENDERER} is the one backend that does override
\texttt{HasRealDepthBuffer} specifically to return \texttt{false} unconditionally (Task~708)
--- the one case in this whole family of defaults where the honest answer requires active
code rather than falling out of inaction, because the naive default (echoing the request back)
would otherwise silently claim a depth buffer exists on a backend whose 2D-only render targets
never allocate one.

\subsection{IEffectBackend and IOcclusionQueryBackend: capability-gated by construction}

These are the two thinnest interfaces in the table above, and both connect directly to
\cnaclass{GraphicsCapability} values already named in this chapter.
\cnaclass{IOcclusionQueryBackend} is fully pure virtual --- \texttt{Begin()}, \texttt{End()},
\texttt{IsComplete()}, \texttt{PixelCount()} --- with no default-no-op escape hatch at the
interface level at all; a backend that does not support occlusion queries simply never
constructs one in the first place, since \texttt{CreateOcclusionQuery()} on
\cnaclass{IGraphicsBackend} itself is what defaults to \texttt{nullptr}
(\cnaclass{GraphicsCapability::OcclusionQuery} is the flag a caller checks before ever
reaching this interface at all).

\cnaclass{IEffectBackend} takes the opposite approach for a subtler reason worth reading
carefully: only \texttt{CompileProgram}, \texttt{Bind}, \texttt{Unbind}, \texttt{IsValid}, and
\texttt{GetCompileError} are pure virtual --- every one of its \texttt{SetUniform*} setters
(\texttt{Float}, \texttt{Int}, \texttt{Vec2/3/4}, \texttt{Mat4}, and the array forms) defaults
to a silent no-op body in the interface itself, the identical shape as
\cnaclass{ISpriteBatchBackend}'s \texttt{SetTransformMatrix} default from this chapter's own
\texttt{ISpriteBatchBackend} section above. This means a backend could, in
principle, compile a \cnaclass{ShaderEffect}'s program successfully (\texttt{IsValid()}
returning \texttt{true}) while silently dropping every single uniform value a game sets on it
--- exactly the kind of ``it compiles and something appears on screen'' risk
Chapter~\ref{ch:shader-fx-gap}'s own \texttt{IsEffectValid()} worked example already warned
readers to check for, extended one level deeper here: a valid program is not the same
guarantee as a program that actually receives the uniforms it was given. Backends that ship
\cnaclass{ShaderEffect} support do \emph{not} uniformly override the full setter set; this is a
real, observed boundary, not merely a hypothetical one. EasyGL supplies every
scalar, vector, matrix, array, and 2D/cube/volume texture hook. D3D11 and D3D12 supply the
scalar/vector/matrix family but inherit both array no-ops and every texture no-op; D3D9 has
the same setter boundary. Their \cnaclass{SpriteBatch} paths bind only the primary texture
expected by the fixed shader contract. Vulkan and SDL GPU likewise implement only their fixed
scalar/vector/matrix slots. Headless records scalar/vector/matrix/array updates and a 2D
texture bind for logic tests, but cannot prove a pixel result. Most strikingly, Software
returns a valid effect while deliberately executing none of the supplied source and
discarding every setter. Chapter~\ref{ch:shader-fx-gap} gives the full live matrix; the
architectural lesson here is that each method on this permissive interface is an independent
capability, even after compilation succeeds.

\subsection{Buffer streaming: one ignored constructor flag, four real hint mappings}

\cnaclass{IVertexBufferBackend} has two permissive defaults that originally made the public
buffer family look simpler than it now is. \path{SetDataWithOptions()} discards its
\cnaclass{SetDataOptions} argument and calls plain \texttt{SetData()}; and
\path{SetVertexDeclaration()} discards the caller's element list. The index interface has
the analogous \path{SetData16WithOptions()} / \path{SetData32WithOptions()} forwarding
defaults. These methods are all reachable on the ten 3D-capable implementations, unlike the
unreachable \texttt{SetData32} throw discussed next.

The shared public constructors impose one universal limit before that backend split. Each
\texttt{Dynamic*} class passes \texttt{dynamic=true} to its protected base constructor, but
that constructor leaves the parameter unnamed and calls the same factory as the static class.
The type therefore never selects a different allocation merely by being dynamic. That does
\emph{not} mean every upload hint is ignored:

\begingroup
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.20\textwidth}p{0.26\textwidth}p{0.46\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Options route} & \textbf{Actual update contract} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Options route} & \textbf{Actual update contract} \\
\midrule
\endhead
\texttt{EASYGL}
  & explicit, distinct & \texttt{Discard} orphans storage then uploads at offset zero;
    \texttt{NoOverwrite} uses sub-data in place. Vertex buffers need an existing allocation.
    Index-buffer \texttt{NoOverwrite} additionally falls back to reallocation when context
    recovery is disabled, because its eligibility check is coupled to the absent CPU shadow. \\
\texttt{SDL\_GPU}
  & explicit, distinct & \texttt{None}/\texttt{Discard} set
    \texttt{SDL\_UploadToGPUBuffer}'s \texttt{cycle} flag; \texttt{NoOverwrite} clears it. \\
\texttt{D3D9}
  & explicit, distinct & Every buffer is native dynamic storage. \texttt{NoOverwrite} uses
    \texttt{D3DLOCK\_NOOVERWRITE}; both other values use \texttt{D3DLOCK\_DISCARD}.
    A freshly created or grown buffer forces discard regardless of the requested option. \\
\texttt{D3D11}
  & explicit, distinct & Every buffer is \texttt{D3D11\_USAGE\_DYNAMIC}.
    \texttt{NoOverwrite} maps to \texttt{D3D11\_MAP\_WRITE\_NO\_OVERWRITE}; both other values
    map to \texttt{D3D11\_MAP\_WRITE\_DISCARD}. \\
\texttt{VULKAN}, \texttt{BGFX}
  & inherited default & The option is discarded. Vulkan copies to persistently mapped memory;
    BGFX updates its dynamic handle at element zero. \\
\texttt{WEBGPU}, \texttt{D3D12}
  & explicit, ignored & WebGPU always calls \texttt{wgpuQueueWriteBuffer}; D3D12 always performs
    its synchronous staging-buffer copy and fence wait. \\
\texttt{SOFTWARE}, \texttt{HEADLESS}
  & explicit, ignored & Software replaces its CPU bytes. Headless records the same generic
    \texttt{SetData} trace, with no option identity retained. \\
\texttt{SDL\_RENDERER}, \texttt{ASCII}, \texttt{CANVAS}, \texttt{DX3}
  & unreachable & Buffer construction fails first under their 2D-only contract. \\
\bottomrule
\end{longtable}
\endgroup

EasyGL has the strongest public pixel proof:
\path{easygl_dynamic_buffer_stress_test.cpp} cycles all three enum values over twelve
successive colored-quad frames and reads the center pixel each time. SDL GPU's smoke test
calls both distinct paths for vertex and index buffers but checks only the resulting count,
so the native \texttt{cycle} distinction is established by source, not by that assertion.
WebGPU's own plan still leaves its option-specific buffer tests open.

One bug sits above every row in this table. The options-taking public wrappers pack and upload
their bytes but never update the shared \cnaclass{VertexBuffer}/\cnaclass{IndexBuffer}
\texttt{cpuShadow\_}. Consequently a non-\texttt{WriteOnly} dynamic buffer's inherited
\texttt{GetData()} cannot return the data just uploaded: on a fresh instance the empty shadow
produces \texttt{ArgumentOutOfRangeException}; after an older ordinary upload it can return
stale data. Chapter~\ref{ch:textures-rendertargets} gives the caller-facing consequence.

The declaration default has one live override too. \path{VertexBuffer::SetDataRaw()} sends
its element list through \path{SetVertexDeclaration()} before upload. EasyGL consumes all
twelve \cnaclass{VertexElementFormat} shapes and binds GLSL attribute locations by declaration
order; the other nine constructible implementations inherit the no-op and remain dependent on
their fixed stride/effect combinations. The EasyGL test is deliberately discriminating even
after the fixed layout family grew: its custom record is 48 bytes, the same total as CNA's PBR
vertex, but has different offsets and a trailing color. Correct diagnostic pixels therefore
prove the declaration won over the fixed 48-byte interpretation.

\subsection{DrawEx fallbacks and the narrow instancing bridge}
\label{sec:drawex-instancing-matrix}

The two ordinary effect-aware defaults look dangerously permissive:
\path{DrawPrimitivesEx()} drops its entire parameter block and calls the colored draw, while
\path{DrawIndexedPrimitivesEx()} does the same to its indexed counterpart. The current
factory graph makes both defaults dead. EasyGL, Vulkan, BGFX, WebGPU, SDL GPU, D3D9/11/12,
Software, and Headless all override both methods. The remaining SDL\_RENDERER, ASCII, Canvas,
and DX3 products are 2D-only: public buffer construction fails first, and their colored
fallback target throws if invoked directly. This is the same useful distinction as the dead
\path{UpdatePixels()} default below: a
permissive body is a maintenance risk, but not a current public data-loss path.

\path{DrawInstancedPrimitivesEx()} is different. Its shared default throws
\path{std::runtime_error}, and is live on two constructible 3D implementations. The public
bridge above it also narrows XNA/FNA's binding model before any backend runs. CNA stores
multiple bindings, but scans out only the first non-null entry with a positive instance
frequency. \path{GpuDrawParams} receives one raw instance-buffer pointer, with no frequency,
binding offset, or additional stream. FNA's \path{PrepareVertexBindingArray()} instead
forwards every binding's buffer, declaration, offset, and frequency to FNA3D. CNA can
therefore represent ``one mesh stream plus one first-element, divisor-one instance stream,''
not the complete public type it stores.

\begingroup
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.14\textwidth}p{0.28\textwidth}p{0.23\textwidth}p{0.27\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Instanced implementation} &
  \textbf{No positive-frequency binding} & \textbf{Strongest located evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Instanced implementation} &
  \textbf{No positive-frequency binding} & \textbf{Strongest located evidence} \\
\midrule
\endhead
\texttt{EASYGL}
  & Real GPU instanced call. A custom \cnaclass{ShaderEffect} binds both caller declarations;
    instance attributes use divisor one. Its stock-effect branch binds no instance attributes.
  & Draws the requested number of identical instances.
  & Public two-instance test reads two distinct pixels and mutation-proves the divisor. It
    deliberately uses a custom effect. \\
\texttt{VULKAN}
  & Fixed instanced pipeline; the instance record supplies a world matrix.
  & Falls back to one ordinary indexed draw.
  & Public three-instance test reads three distinct positions plus background. \\
\texttt{BGFX}
  & Fixed instanced program and transient instance-data buffer; insufficient transient
    capacity also returns silently.
  & Returns without drawing.
  & Source only; no dedicated BGFX instancing test was found. \\
\texttt{WEBGPU}
  & Fixed two-stream pipeline and copied instance-buffer shadow.
  & Falls back to one ordinary indexed draw.
  & Direct-backend three-instance pixel test; its separate fallback check also reads a pixel. \\
\texttt{D3D9}
  & Fixed 64-byte world-matrix stream using \texttt{SetStreamSourceFreq}; resets persistent
    stream frequency after drawing.
  & Falls back to one ordinary indexed draw.
  & Direct-backend two-instance pixels, fallback behavior, and post-draw reset are
    mutation-tested. \\
\texttt{D3D11}
  & Fixed 64-byte \texttt{INSTANCEWORLD0--3} input and
    \texttt{DrawIndexedInstanced}.
  & Falls back to one ordinary indexed draw.
  & Direct smoke test reads exact output for one identity instance; it does not prove
    multi-instance stepping. \\
\texttt{D3D12}
  & Fixed dual-stream instanced PSO; currently requires an offscreen color target.
  & Falls back to one ordinary indexed draw.
  & Direct smoke test reads exact output for one identity instance only. \\
\texttt{SDL\_GPU}
  & Inherits the shared throwing default despite having real 3D buffers and pipelines.
  & Same unconditional throw.
  & Source-proven; no dedicated instancing test was found. \\
\texttt{SOFTWARE}
  & Inherits the shared throwing default despite real CPU vertex/index buffers.
  & Same unconditional throw.
  & Source-proven; no dedicated instancing test was found. \\
\texttt{HEADLESS}
  & Logic-only override: records one indexed draw and increments its draw-call statistic to
    the requested instance count.
  & Records the same logical count without requiring instance data.
  & Source only; no dedicated public binding/instancing test was found. \\
\textit{Four 2D-only products}
  & Shared unconditional throw.
  & Same throw.
  & Public construction of the required buffers fails earlier under the 2D-only contract. \\
\bottomrule
\end{longtable}
\endgroup

The seven real GPU callbacks do not imply a portable arbitrary-effect contract. Except for
EasyGL's custom-effect branch, each chooses a CNA-owned instancing shader rather than making
the currently applied effect consume the caller's declarations. Their common useful subset is
a per-vertex position stream, one per-instance world matrix, shared view/projection, and a
small fixed material payload. This is why a valid stock or custom effect does not by itself
establish that the same instanced scene is portable across these rows.

The binding state has an independent defect above this matrix: the offset-taking singular
setter discards its offset, and both singular setters write only
the singular buffer pointer; the old binding vector survives. Conversely,
\path{SetVertexBuffers({})} clears the vector while leaving that pointer live. The plural
getter can therefore disagree with the singular getter, and replacing a mesh through the
singular setter after an earlier instanced binding can reuse the stale instance buffer on the
next instanced draw. The existing \path{VertexBufferBindingTests.cpp} checks value copying in
a standalone vector, never these \cnaclass{GraphicsDevice} transitions.

Finally, \cnaclass{GraphicsCapability} has no hardware-instancing entry. This is not merely
the usual false-positive-default problem: there is no query value to override. FNA asks
FNA3D directly and throws \texttt{NoSuitableGraphicsDeviceException} when hardware
instancing is unavailable; CNA callers cannot perform an equivalent portable preflight and
will instead see the row-specific throw, fallback, repeated draw, or silent return above.

\subsection{Texture2D updates: one dead default and one live silent discard}
\label{sec:texture2d-update-matrix}

\cnaclass{ITextureBackend} gives both \texttt{UpdatePixels()} and
\texttt{UpdatePixelsLevel()} empty default bodies. They look symmetric in the header, but
the current factory graph makes them materially different. Every concrete object returned by
any backend's \texttt{CreateTexture()} overrides level~0 \texttt{UpdatePixels()} (ASCII
returns SDL\_RENDERER's object), so that first no-op is unreachable today. The mip-level
default remains live on SDL GPU. Software and Headless override it too, but deliberately
discard the bytes or record only a trace, respectively.

The public routing matters before reading the table. Whole-texture
\texttt{SetData(Color*, count)} recreates the resource and never calls either update hook.
Only the detailed level/rectangle overload reaches them: level~0 patches the CPU shadow and
re-uploads that complete level through \texttt{UpdatePixels()}, while a higher level calls
\texttt{UpdatePixelsLevel()}. Ordinary \texttt{Texture2D::GetData()} then reads the same CPU
shadow, not the GPU object, so a public SetData/GetData round trip is not proof that either
hook changed sampled content.

\begingroup
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.16\textwidth}p{0.20\textwidth}p{0.25\textwidth}p{0.31\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Detailed level~0} & \textbf{Level above zero} &
  \textbf{Strongest located evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Detailed level~0} & \textbf{Level above zero} &
  \textbf{Strongest located evidence} \\
\midrule
\endhead
\texttt{EASYGL}
  & real full-level GPU upload & real GPU mip upload
  & A rendered minified stock-effect pixel selects authored higher-level color. The older
    SetData/GetData test is CPU-shadow-only. \\
\texttt{VULKAN}
  & real staged GPU upload & real staged, level-specific GPU upload
  & A rendered minified pixel selects the authored mip; validation also caught and fixed a
    wrong-level image barrier. \\
\texttt{BGFX}
  & real mutable-texture update & real \texttt{updateTexture2D} mip upload
  & A rendered minified pixel selects the authored mip. \\
\texttt{WEBGPU}
  & real upload; also regenerates the chain & real per-level upload
  & Direct backend readback distinguishes level~1, and a nonuniform source proves
    linear-filtered generated mips. \\
\texttt{SDL\_GPU}
  & real transfer-buffer upload & inherited no-op; physical texture has one level
  & No plain-Texture2D mip test was found. The one-level descriptor and missing override are
    source-proven. \\
\texttt{D3D9}
  & real managed-texture lock & real per-level \texttt{LockRect}
  & Exact native readback proves construction and a level~0 replacement; higher-level
    addressing is source-proven. \\
\texttt{D3D11}
  & real \texttt{UpdateSubresource} & real mip-subresource update
  & Direct staging readback proves exact level~1 bytes and unchanged level~0. \\
\texttt{D3D12}
  & real staged copy and fence & real mip-subresource copy
  & Direct readback proves exact level~1 bytes and unchanged level~0. \\
\texttt{SOFTWARE}
  & real CPU level~0 replacement & explicit no-op
  & Source only for the mip discard; the public shadow can mask it. \\
\texttt{HEADLESS}
  & real CPU level~0 replacement plus trace & trace only; no backend mip storage
  & Logic trace only; no sampled-pixel claim is possible. \\
\texttt{SDL\_RENDERER}, \texttt{ASCII}
  & real SDL texture update & explicit \texttt{runtime\_error}
  & SDL\_RENDERER's dedicated test proves the throw; ASCII returns the same texture class. \\
\texttt{CANVAS}
  & real Canvas2D update & explicit \texttt{runtime\_error}
  & Source-proven; no dedicated mip test was found. \\
\texttt{DX3}
  & real DirectDraw surface write & explicit \texttt{runtime\_error}
  & Source-proven; no dedicated mip test was found. \\
\bottomrule
\end{longtable}
\endgroup

WebGPU's level~0 behavior is a deliberate compatibility difference, not merely an
implementation technique. It renders a linear downsample from each level into the next
whenever level~0 changes, including at construction. FNA and CNA's other ordinary-texture
implementations expect authored/content-pipeline mip data instead. Consequently WebGPU gives
an otherwise-empty chain useful pixels automatically, but a later level~0 write also
regenerates over higher levels authored before it.

The test boundary is especially important for EasyGL and Vulkan's identically registered
\path{easygl_texture2d_mip_test.cpp}: the file itself states ``No framebuffer readback ---
pure CPU shadow buffer.'' The separate texture-filter tests are the GPU proof. D3D11/12 and
WebGPU instead bypass the public shadow and read native subresources directly. This is the
same audit rule the volume/cube matrix below applies to Headless's misleadingly named
``round-trip'' tests: follow the assertion to the storage layer it actually observes.

\subsection{Volume and cube readback: the no-op default mostly became real}
\label{sec:texture-volume-readback-matrix}

\cnaclass{ITexture3DBackend::GetData()} and
\cnaclass{ITextureCubeBackend::GetData()} both default to an empty body. The public wrappers
make the consequence subtler than ``the caller is untouched.'' When a backend object exists,
each wrapper allocates a zero-initialized RGBA byte vector, calls the backend, then converts
the whole requested element range back to \cnaclass{Color}. An inherited no-op therefore
\emph{overwrites} that destination range with transparent black. When a resource factory
returns \texttt{nullptr}, by contrast, the wrapper returns before allocating or converting
anything and really does leave the caller's destination unchanged.

The ordinary-resource columns below cover \cnaclass{Texture3D} and
\cnaclass{TextureCube}. The third is separate because \cnaclass{RenderTargetCube} inherits
the same public \texttt{GetData()} but supplies a different backend object:

\begingroup
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.17\textwidth}p{0.22\textwidth}p{0.22\textwidth}p{0.31\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{Texture3D} & \textbf{TextureCube} & \textbf{RenderTargetCube} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{Texture3D} & \textbf{TextureCube} & \textbf{RenderTargetCube} \\
\midrule
\endhead
\texttt{EASYGL}
  & real GPU readback & real GPU readback & inherited no-op; transparent black \\
\texttt{VULKAN}
  & real GPU readback & real GPU readback & inherited no-op; transparent black \\
\texttt{BGFX}
  & real if transfer caps exist & real if transfer caps exist
  & inherited no-op; transparent black \\
\texttt{D3D9}
  & real GPU readback & real GPU readback & inherited no-op; transparent black \\
\texttt{D3D11}
  & real GPU readback & real GPU readback & inherited no-op; transparent black \\
\texttt{D3D12}
  & real GPU readback & real GPU readback & inherited no-op; transparent black \\
\texttt{WEBGPU}
  & real GPU readback & real GPU readback & real GPU readback; level~0 only \\
\texttt{SDL\_GPU}
  & real GPU readback & real GPU readback & real GPU readback \\
\texttt{SOFTWARE}
  & null factory; caller unchanged & real CPU readback; level~0
  & null factory; caller unchanged \\
\texttt{HEADLESS}
  & explicit transparent black & explicit transparent black
  & inherited no-op; transparent black \\
\texttt{SDL\_RENDERER}, \texttt{ASCII}, \texttt{CANVAS}, \texttt{DX3}
  & null factory; caller unchanged & null factory; caller unchanged
  & null factory; caller unchanged \\
\bottomrule
\end{longtable}
\endgroup

The eight ordinary GPU implementations are not CPU-shadow echoes. EasyGL attaches the
requested slice or face to a temporary framebuffer and calls \texttt{glReadPixels}; Vulkan
copies the requested image subresource into host-visible staging memory; D3D9 locks its
managed cube/volume resource, while D3D11 and D3D12 use staging/readback resources with
native row and slice pitches; WebGPU maps a staged copy asynchronously; and SDL GPU waits on
a download transfer and fence. BGFX cannot mark an ordinary sampled source
\texttt{READ\_BACK}, so it blits the requested region into a temporary transfer texture and
advances frames until \texttt{readTexture()} completes. If the selected renderer lacks its
required blit/readback capabilities, that path logs and returns; the public zero-initialized
staging vector then makes the result transparent black.

The proof is correspondingly stronger than ``did not throw.'' EasyGL, Vulkan, and BGFX
share exact slice, off-origin rectangle, and mip round trips; WebGPU's public checks
distinguish all six faces, a non-origin rectangle, and mip data, plus separate volume slices,
voxel, and mip cases. SDL GPU checks distinct slices/faces and authored/generated mip
retention. D3D9/11/12 smoke tests compare exact subregion bytes (D3D9 first gates its
volume/cube cases on the real device caps). Software's cube storage is source-proven and its
environment-map sampling is pixel-proven, but no dedicated public exact
\texttt{TextureCube::GetData()} assertion was found.

Headless is the cautionary counterexample. The first comments in
\path{headless_resource_backends_test.cpp} call its volume and cube checks ``round-trips,''
but the implementation deliberately discards uploads and returns zero placeholders. The
actual assertions only prove that calls on two faces and one volume do not throw; they never
compare returned colors. A test title or introductory comment is not evidence when the
assertion body establishes a weaker contract.

\subsection{RenderTargetCube: one inherited upload, two independent transition traps}
\label{sec:rendertargetcube-default-contracts}

\cnaclass{RenderTargetCube} inherits \cnaclass{TextureCube}'s complete public
\texttt{SetData()} family. FNA leaves that inherited call live and forwards it to
\texttt{FNA3D\_SetTextureDataCube()}, including when the texture was created as a render
target. CNA's backend contract instead redeclares \texttt{SetData()} on
\cnaclass{IRenderTargetCubeBackend} with an empty default body and the comment ``render targets
do not support CPU-side SetData.'' Only EasyGL overrides it. The shared public wrapper still
performs face, rectangle, mip-level, pointer, and count validation and still converts the
requested \cnaclass{Color} range to RGBA bytes before making that call. Consequently a valid
upload reports success everywhere: EasyGL changes the requested face/subregion; every other
constructed cube target silently discards the bytes. Software and the four 2D-only backends
construct no cube target, so their public object has a null backend and discards the upload
one layer earlier. No dedicated test calls \texttt{SetData()} on a
\cnaclass{RenderTargetCube}; the EasyGL result and every no-op are source-proven only.

Target switching is a separate default. \cnaclass{IGraphicsBackend}'s
\texttt{SetRenderTargetCubeFace(rt, face)} calls
\texttt{rt->BindAsRenderTargetFace(face)} when non-null, but on null merely delegates to
\texttt{SetRenderTarget2D(nullptr)}. It never remembers or calls the outgoing cube target's
\texttt{UnbindAsRenderTarget()}. That is harmless only when the backend either overrides the
method, tracks cube state elsewhere, or finalizes each face without an unbind callback:

\begingroup
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.16\textwidth}p{0.16\textwidth}p{0.60\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{RT-cube SetData} & \textbf{Public cube-target transition contract} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{RT-cube SetData} & \textbf{Public cube-target transition contract} \\
\midrule
\endhead
\texttt{EASYGL}
  & real GPU upload & Explicit tracking finalizes on cube-to-2D/backbuffer and between different
    cube objects. Direct face-to-face switching on the \emph{same} cube skips unbind. That is
    safe for single-sample level~0 and final whole-cube mip generation, but a shared MSAA
    renderbuffer means only the last face is resolved unless the caller unbinds between faces. \\
\texttt{VULKAN}
  & no-op & The common route is sound: deferred draw commands retain their face proxy, and
    command recording performs per-face render-pass resolve and mip generation independently
    of the cube object's unbind callback. \\
\texttt{BGFX}
  & no-op & Ordinary unbind through the 2D-null overload restores view~0, and BGFX performs
    attachment resolve/mip work. Its explicit cube-null overload is broken: the backend override
    returns immediately on null. All six faces also share one view ID, so same-frame face
    rendering still needs the test suite's forced frame boundary. \\
\texttt{D3D9}
  & no-op & An explicit override tracks the cube and unbinds it on every transition, including
    face-to-face; resolve and backbuffer restoration therefore run. \\
\texttt{D3D11}
  & no-op & Broken. The common bind never populates the backend's 2D-only
    \texttt{currentCustomRT\_}; the later 2D-null call neither invokes the cube's resolve/mip
    callback nor restores the backbuffer. Subsequent backbuffer work can still target the cube. \\
\texttt{D3D12}
  & no-op & Partly broken. The 2D-null path restores the backbuffer unconditionally, but no cube
    is tracked, so its \texttt{ResolveMsaaEXT()} and \texttt{GenerateMipsEXT()} callbacks never
    run through the public API. Single-sample level~0 rendering survives this omission. \\
\texttt{WEBGPU}
  & no-op & Sound within its declared scope: binding another face or target eagerly flushes the
    outgoing target identity. Cube-target mipmaps are rejected and cube MSAA reports zero. \\
\texttt{SDL\_GPU}
  & no-op & Sound: mutually exclusive 2D/cube pointers and queued per-face state preserve
    face-to-face work; public tests prove cube MSAA resolve and generated mip content. \\
\texttt{HEADLESS}
  & no-op & The common route changes logical trace state only; it cannot establish pixel,
    resolve, or mip behavior. \\
\texttt{SOFTWARE}, \texttt{SDL\_RENDERER}, \texttt{ASCII}, \texttt{CANVAS}, \texttt{DX3}
  & null backend & No cube render-target factory, so no native transition or upload occurs. \\
\bottomrule
\end{longtable}
\endgroup

The Direct3D distinction is especially easy to miss because both smoke suites prove the
underlying algorithms with direct backend calls:
\texttt{BindAsRenderTargetFace()} followed by \texttt{UnbindAsRenderTarget()}. Those tests
establish that the resolve and mip implementations work \emph{when invoked}; no D3D11/12 test
was found that routes a cube through \cnaclass{GraphicsDevice}'s public cube-target overload
and then back through the public null-target call. They therefore cannot prove the public
transition that currently fails to invoke them. This is the same audit lesson as the Headless
``round-trip'' above: inspect the call layer the test exercises, not only its feature name.

\subsection{IIndexBufferBackend::SetData32: a defensive throw traced out to dead code}

Not every default in this file is a silent no-op. \cnaclass{IIndexBufferBackend}'s
\texttt{SetData32} defaults to throwing a plain \texttt{std::runtime\_error}
(\texttt{"SetData32 not supported by this backend"}) rather than quietly doing nothing --- the
opposite failure mode from every default this chapter has covered so far. Grepping every
backend's own implementation directory shows exactly four backends never override it:
\texttt{SDL\_RENDERER}, \texttt{ASCII}, \texttt{CANVAS}, and \texttt{DX3}; the other ten
(\texttt{EasyGL}, \texttt{Vulkan}, \texttt{BGFX}, \texttt{D3D9}, \texttt{D3D11}, \texttt{D3D12},
\texttt{WebGPU}, \texttt{SdlGpu}, \texttt{Software}, and the internal \texttt{Headless} test
backend) all supply a real one.

Tracing where that default would actually fire tells a reassuring story rather than an
alarming one, though only once actually traced rather than assumed: all four non-overriding
backends are the project's 2D-only backends, and every one of them already refuses to
construct \emph{any} index buffer at all, 16-bit or 32-bit, at its own
\texttt{CreateIndexBuffer16} / \texttt{CreateVertexBuffer} factory call --- confirmed directly
in \texttt{Dx3GraphicsBackend.cpp} and \texttt{CanvasGraphicsBackend.cpp} (\texttt{"does not
support 3D"} thrown by name for both calls) and in a real, dedicated regression test for
\texttt{SDL\_RENDERER} (\texttt{sdlrenderer\_buffer\_construction\_throws\_test.cpp}, Task~723)
that pins the exact thrown message for a \texttt{ThirtyTwoBits} \cnaclass{IndexBuffer}
construction --- and that message is \texttt{CreateIndexBuffer16}'s own, not a distinct
32-bit-specific one, since \texttt{CreateIndexBuffer32} is never separately overridden on this
backend either and simply delegates to the 16-bit path's throw. \texttt{ASCII} inherits the
identical no-3D-support story from \texttt{SDL\_RENDERER} underneath it
(Chapter~\ref{ch:what-is-cna}'s own verification-scale table already notes this inheritance).
The practical consequence: \cnaclass{IIndexBufferBackend}'s own \texttt{SetData32} default can
never actually fire in this project today --- every backend that would reach it fails earlier,
at construction, for an unrelated and already-well-tested reason, and every backend that
survives construction already supplies its own real override. It is defensive code with no
live call path behind it right now, not a currently-reachable gap --- worth recording precisely
as that, rather than either dismissing it as obviously dead without checking or citing it as an
active risk without tracing where it actually leads.

\section{ClearOptions: seven shared routes, fourteen execution models}
\label{sec:backend-clear-matrix}

\cnaclass{GraphicsDevice} converts the colour once, then selects exactly one of seven backend
methods for the seven non-empty combinations of target/depth/stencil. Before that selection it
validates a requested depth value and applies the active-target mask described in
\S\ref{sec:clearoptions}. FNA's mask uses the actual \cnaclass{DepthFormat}: \texttt{None}
retains target only, while every format except \texttt{Depth24Stencil8} removes stencil. CNA
currently has only a binary depth-storage query, and its singular cube binding is absent from
the tracked target list. The table begins \emph{after} those shared differences; ``exact'' means
exact only for the flags that survived shared routing.

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{@{}p{0.13\textwidth}p{0.25\textwidth}p{0.27\textwidth}p{0.25\textwidth}@{}}
\toprule
\textbf{Product} & \textbf{Native/pass behavior} & \textbf{Option and target fidelity} &
\textbf{Evidence boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Product} & \textbf{Native/pass behavior} & \textbf{Option and target fidelity} &
\textbf{Evidence boundary} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak{}RENDERER}
  & Immediate \texttt{SDL\_RenderClear} of the active colour target.
  & Public depth/stencil bits are masked away; depth/stencil-only is a silent no-op.
    Direct six 3D hooks throw, but are unreachable through this public path.
  & Two dedicated public tests still expect the obsolete throw behavior and fail. Their
    colour assertions remain useful; Task~1113's policy question is settled by FNA. \\
\texttt{ASCII}
  & Delegates colour and all six combination hooks to the inner SDL renderer.
  & Same public mask and no-op behavior; colour affects the off-screen game target before
    character conversion.
  & The dedicated test proves direct hooks throw, not that public masked calls throw. \\
\texttt{EASYGL}
  & Immediate \texttt{glClear}; viewport-independent but narrowed by an enabled scissor.
    Depth/stencil-specific hooks force full write masks.
  & The plain colour hook incorrectly includes \texttt{GL\_DEPTH\_BUFFER\_BIT} without forcing
    depth writes. Thus public \texttt{Target} and the raw-float overload can clear depth too,
    or fail to do so according to the prior depth mask. Other combinations select their bits.
  & Public overload/range tests, generic depth tests, and cross-frame stencil pixel proof
    exist. No test discriminates target-only depth preservation, depth-only formats, or the
    cube-format mask. \\
\texttt{VULKAN}
  & Deferred render-pass clears. The backbuffer and discard-content targets clear every
    existing attachment at pass start from final global values.
  & Not option-selective. A preserve-content target loads colour and uses
    \texttt{DONT\_CARE} for depth/stencil even after an explicit clear; \texttt{ClearDepth}
    also fails to mark an otherwise-unused target. Calls before queued draws are reordered to
    pass start.
  & Cross-frame depth/stencil tests prove values on the backbuffer; the clear-only RT
    regression proves Task~875's pass is now recorded for the default discard case. Neither
    proves preserve-content or same-pass ordering. \\
\texttt{BGFX}
  & Every prepared view installs
    \texttt{COLOR|DEPTH|STENCIL} through \texttt{setViewClear}; values are global and apply
    once when the view executes.
  & Not option-selective. \texttt{ClearDepth} changes only the scalar and does not touch an
    otherwise-empty view; with a later submission all three attachments clear. Multiple calls
    collapse to final values before all submissions.
  & Generic colour+depth and cross-frame stencil tests pass. They deliberately do not exercise
    depth-only no-draw behavior or same-view issue ordering. \\
\texttt{WEBGPU}
  & Backbuffer and 2D-target passes use independent pending colour/depth/stencil load ops;
    a discard target forces all three. Calls are consumed when the outgoing target/pass flushes.
  & Selective for backbuffer and preserve-content 2D targets after shared masking.
    Cube passes always clear all three attachments because the backend interface did not convey
    cube preserve usage; final global values win there.
  & Same-call colour readback and combined 2D-target depth usage are proven. No isolated
    depth/stencil or cube-option test was found. \\
\texttt{SDL\_GPU}
  & Independent pending flags and values exist per backbuffer, 2D target/MRT attachment, and
    cube face; each becomes the matching SDL GPU load operation when the deferred pass begins.
  & Selective after shared routing. A pass still collapses repeated calls before queued draws,
    and shared code cannot inspect a singular cube's format.
  & Smoke/render-state tests exercise the full combined form; no isolated option-order or
    cube-format discriminator was found. \\
\texttt{D3D11}
  & Immediate full-view clears: all bound colour RTVs, plus exactly requested native
    depth/stencil flags when a DSV exists.
  & Backend selection is exact. Shared routing wrongly retains stencil for
    \texttt{Depth16}/\texttt{Depth24}; the former has no stencil plane, while the latter is
    physically mapped to D24S8 despite its depth-only public name.
  & Strong direct-backend GPU readback proves stencil values and depth-only colour
    preservation; ordinary public tests do not cover the format-mask edge. \\
\texttt{D3D12}
  & Immediate, synchronous command lists clear bound off-screen/swap-chain RTVs and selected
    DSV flags exactly.
  & Same format-mask issue as D3D11. In an off-screen device with no colour resource bound,
    every combination helper checks that colour binding first, so even a depth-only no-op
    throws \texttt{NotYetImplemented}.
  & Strong direct-backend depth-test and stencil-plane readback proof exists. It bypasses the
    shared public mask and requires a real bound target. \\
\texttt{D3D9}
  & Immediate \texttt{IDirect3DDevice9::Clear} with the selected
    \texttt{D3DCLEAR\_*} bits.
  & Native bits are further gated by the presentation/backbuffer
    \texttt{depthStencilFormatOrdinal\_}, even while a differently-formatted 2D/cube target is
    bound. The active attachment can therefore be skipped or receive an invalid extra bit.
  & The smoke test calls all seven public combinations on D24S8, but most depth/stencil-only
    checks prove only that colour stayed unchanged. No backbuffer/target-format mismatch test
    exists. \\
\texttt{SOFTWARE}
  & Immediate CPU framebuffer clears with independent colour and depth arrays.
  & Colour/depth selection is exact after shared routing. There is no stencil storage:
    stencil-only is a no-op and combined forms ignore that component.
  & Source-proven; broad rendering tests use clears, but no dedicated seven-combination
    discriminator was found. \\
\texttt{HEADLESS}
  & Synthetic state only: colour forms update last-clear colour and one counter; depth/stencil
    forms add counters/traces without pixels.
  & Dispatch identity is visible in traces, not attachment contents. Backend-wide depth support
    defaults true; per-target support merely echoes whether depth was requested.
  & Smoke and trace-difference tests prove counters/names, not XNA rendering semantics. \\
\texttt{CANVAS}
  & Browser builds synchronously overwrite the whole active Canvas2D context (transparent
    clears use \texttt{clearRect}); native non-Emscripten builds do nothing.
  & Public depth/stencil is masked away. Direct combination hooks throw as unsupported 3D, but
    public calls cannot reach them.
  & Source-audited; no browser execution was performed for this audit. \\
\texttt{DX3}
  & Immediate locked full-surface RGBA fill of the active free-direct target, including caller
    alpha.
  & Public depth/stencil is masked away and becomes a no-op; direct 3D combination hooks throw.
  & Dedicated tests prove exact colour/alpha, target redirection, public no-op masking, and the
    otherwise-unreachable direct throws. \\
\bottomrule
\end{longtable}
\endgroup

No \cnaclass{GraphicsCapability} value reports clear selectivity or same-pass ordering.
Portable code should use \texttt{Clear(Color)} for the conventional frame-start operation,
avoid the raw-float overload, pass only attachments the active target publicly owns, and place
clear calls before drawing. Even that discipline cannot repair Vulkan preserve-content clears,
EasyGL target-only depth coupling, or a backend's cube-format routing; those are implementation
gaps, not caller preconditions.

\section{Viewport and scissor: one stored contract, fourteen raster contracts}
\label{sec:backend-viewport-scissor-matrix}

\cnaclass{GraphicsDevice} stores both public values before immediately invoking the two
otherwise-empty backend hooks. Scissor enable arrives separately through
\texttt{ApplyRasterizerState}. A successful setter and an exact getter round trip therefore say
nothing yet about where a later primitive lands. The table records \emph{draw-time} behavior;
clear operations are a separate native-API question.

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\begin{longtable}{@{}p{0.13\textwidth}p{0.22\textwidth}p{0.27\textwidth}p{0.28\textwidth}@{}}
\toprule
\textbf{Product} & \textbf{Custom viewport} & \textbf{Scissor rectangle + enable} &
\textbf{Scope, timing, and evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Product} & \textbf{Custom viewport} & \textbf{Scissor rectangle + enable} &
\textbf{Scope, timing, and evidence} \\
\midrule
\endhead
\texttt{SDL\_\allowbreak{}RENDERER}
  & No hook; stored property and \cnaclass{Viewport} math only.
  & A positive rectangle immediately installs an SDL clip; a non-positive size disables it.
    \texttt{ScissorTestEnable} is ignored, so a non-empty rectangle clips even when false.
  & Immediate 2D-renderer state. The rasterizer test proves assignment/round trip only; no
    clipping or viewport pixel test was found. \\
\texttt{ASCII}
  & No hook.
  & Delegates the rectangle to its inner SDL renderer, inheriting the same always-active clip;
    the rasterizer Boolean is not delegated.
  & Clips the off-screen game target before glyph quantization. Source-proven, with no dedicated
    state pixel test. \\
\texttt{EASYGL}
  & Immediate GL viewport and depth range, with top-left to bottom-left conversion using the
    active target height.
  & Immediate GL rectangle plus a genuinely independent GL enable.
  & Dedicated 3D viewport and scissor pixel tests, plus target-switch reset proof. A SpriteBatch
    flush overwrites the GL viewport with full window/target size and does not restore it; its
    scissor remains effective. \\
\texttt{VULKAN}
  & Stored until command recording; custom value is read for the backbuffer pass only.
  & Stored Boolean/rectangle is read for the backbuffer pass only; target passes force their
    full rectangle.
  & One live value governs all queued sprites and 3D draws when the pass is recorded, not each
    draw's issue-time value. Both features have backbuffer pixel tests; target-reset proof does
    not prove a custom target sub-region. \\
\texttt{BGFX}
  & Applied before each backbuffer 3D submit only; SpriteBatch and render-target views remain
    full-size. Min/max depth is discarded.
  & Independent Boolean and rectangle, re-applied before every SpriteBatch and 3D submit.
  & Dedicated public pixel tests prove both features. Scissor is per submit; viewport is a
    per-view rectangle with the stated 3D/backbuffer limit. \\
\texttt{WEBGPU}
  & Stored, clamped to target bounds, and applied once when each pass is encoded.
  & Independent Boolean/rectangle; disabled or zero becomes the full target, otherwise it is
    clamped and applied once per pass.
  & Backbuffer, 2D-target, and cube passes use the live record-time value for all queued sprite
    and 3D commands. \texttt{WebGPU\_GraphicsState} has exact inside/outside pixel checks. \\
\texttt{SDL\_GPU}
  & No hook.
  & Independent Boolean/rectangle, applied once per backbuffer, 2D-target, or cube pass; disabled
    or zero becomes full target.
  & Live pass-flush state, not part of each queued command's otherwise-rich snapshot. The
    render-state test pixel-proves a half-target clip. \\
\texttt{D3D11}
  & Immediate \texttt{RSSetViewports}, including depth range.
  & Immediate \texttt{RSSetScissorRects}; cached rasterizer state controls
    \texttt{ScissorEnable}.
  & Persists into subsequent 3D and SpriteBatch draws. Smoke Check~O reads the native state back,
    but calls backend methods directly and is not a behavioral public pixel test. \\
\texttt{D3D12}
  & Empty hook; every 3D and SpriteBatch command list explicitly installs the full target.
  & Empty hook; every draw installs a full-target native scissor regardless of the public
    rectangle/Boolean.
  & Source-proven negative contract. State-object pixel tests cover other fields, not a custom
    viewport/scissor discriminator. \\
\texttt{D3D9}
  & Immediate native viewport, including depth range.
  & Immediate native rectangle; \texttt{D3DRS\_SCISSORTESTENABLE} is independent.
  & Subsequent 3D/SpriteBatch draws use native state. Source and broad native-draw tests exist,
    but the 39-scene oracle corpus contains no viewport/scissor scene; the plan's
    ``oracle-proven'' shorthand is stale. \\
\texttt{SOFTWARE}
  & Explicit no-op.
  & Explicit rectangle no-op; rasterizer application keeps culling but discards scissor enable.
  & Public values round-trip while both CPU 3D and SpriteBatch rasterization ignore them.
    Source-proven; no dedicated negative test. \\
\texttt{HEADLESS}
  & Validation and trace only.
  & Validation and trace only; enable is merely another traced rasterizer-state change.
  & No pixels are rasterized. Tests prove viewport trace divergence and validation-mode rejection
    of invalid scissor input, not visual clipping. \\
\texttt{CANVAS}
  & No hook.
  & No rectangle or rasterizer hook.
  & Stored-only public state on a 2D-only browser backend; source-audited, not browser-executed. \\
\texttt{DX3}
  & No hook.
  & No rectangle or rasterizer hook.
  & Stored-only public state on the 2D \texttt{free-direct} route; no dedicated test. \\
\bottomrule
\end{longtable}
\endgroup

The common lifecycle adds two independent qualifications to every row. Render-target switches
do reset both public values to the first target's or backbuffer's full bounds. Construction and
\texttt{Reset()} do not yet match FNA: CNA starts with a zero public scissor, and its reset path
preserves both custom values when size is unchanged or resets only the viewport after a real
size change. FNA initializes both to presentation bounds and unconditionally resets both.

\cnaclass{SpriteBatch} adds another shared gap above the table. The rasterizer pointer in its
full \texttt{Begin()} overload is never read, whereas FNA
stores the caller's state (defaulting null to \texttt{CullCounterClockwise}) and applies it
before drawing. A capable row therefore still cannot be reached through CNA's ordinary
``pass a scissor-enabled rasterizer to Begin'' idiom; the game must assign device rasterizer
state separately. There is no \cnaclass{GraphicsCapability} entry for either feature, and
invalid rectangles are not normalized at the shared boundary, so portable code must neither
probe by getter nor rely on one backend's clamp/ignore/throw behavior.

\section{GpuDrawParams: already covered, worth remembering here}

Chapter~\ref{ch:stock-effects} already introduced \cnaclass{GpuDrawParams} in full --- the
single struct \texttt{Effect::FillGpuDrawParams()} populates and every backend's
\texttt{DrawPrimitivesEx} / \texttt{DrawIndexedPrimitivesEx} consumes. It is worth restating
here only because it is the concrete mechanism by which ``a stock effect draws identically
across twelve backends'' is actually achieved: the effect layer never talks to a backend
directly about lights, fog, or skinning palettes --- it fills one struct, and each backend's
own draw dispatch decides how to turn that struct into real GPU state.

\section{GraphicsCapability: ask, don't catch}
\label{sec:graphicscapability}

Rather than requiring game code to attempt an operation and catch an exception to discover
whether a backend supports it, \cnaclass{GraphicsCapability} is a small enum ---
\texttt{ThreeD}, \texttt{DepthStencilBuffer}, \texttt{MultiSampleAntiAliasing},
\texttt{MultipleRenderTargets}, \texttt{AnisotropicFiltering}, \texttt{WireFrame},
\texttt{OcclusionQuery}, \texttt{CustomEffects} --- queryable via
\texttt{GraphicsDevice::\allowbreak{}SupportsCapability\allowbreak{}(GraphicsCapability)}. The default implementation
returns \texttt{true} for everything; a 2D-only backend like \texttt{SDL\_RENDERER}
(Chapter~\ref{ch:sdl-renderer}) overrides this to honestly report what it cannot do, rather
than accepting a 3D draw call and throwing only once the call actually arrives.

\begin{lstlisting}
const auto backend = device.GetGraphicsBackendType();
const bool knownAscii3DFalsePositive =
    backend == GraphicsBackendType::Ascii;
if (device.SupportsCapability(GraphicsCapability::ThreeD) &&
    !knownAscii3DFalsePositive) {
    DrawSceneWith3DModels();
} else {
    DrawFlat2DFallbackScene(); // e.g. running on SDL_RENDERER or CANVAS
}

const bool knownOcclusionFalsePositive =
    backend == GraphicsBackendType::WebGPU ||
    backend == GraphicsBackendType::SdlGpu ||
    backend == GraphicsBackendType::Software ||
    backend == GraphicsBackendType::Ascii;
if (!device.SupportsCapability(GraphicsCapability::OcclusionQuery) ||
    knownOcclusionFalsePositive) {
    // Skip GPU occlusion culling entirely rather than constructing an OcclusionQuery
    // whose factory would return nullptr or throw on these known exceptions.
}
\end{lstlisting}

This is precisely the ``ask, don't catch'' pattern this section's own title promises, and a
Chapter~\ref{ch:graphicsdevice} finding is worth recalling here as the reason it must be used
deliberately rather than assumed reliable everywhere: \cnaclass{MultipleRenderTargets}
is one of the eight values this mechanism covers, but no backend's \texttt{SupportsCapability}
override actually gates on it, so it silently returns \texttt{true} even on backends (like
\texttt{SOFTWARE}) where a real MRT draw demonstrably fails --- a capability flag that exists,
but that this specific query cannot yet be trusted to answer honestly for.

The source audit confirms further failures of that default-true design. First,
\texttt{ASCII} is 2D-only and forwards its executable 3D calls to SDL renderer's throwing
implementation, but it has no \texttt{SupportsCapability()} override of its own; unlike
\texttt{SDL\_RENDERER}, \texttt{DX3}, and \texttt{CANVAS}, it therefore reports
\texttt{ThreeD=true}. Second, \texttt{CustomEffects} reports true on BGFX, WebGPU, ASCII, and
Software despite four different non-support shapes: an always-invalid compiled object, a null
factory plus a throwing \cnaclass{SpriteBatch}, the same null/throw combination through the
ASCII decorator, and a valid-looking Software object whose source is never executed. Thus
\texttt{OcclusionQuery} is a third confirmed case: WebGPU, SDL GPU, and Software inherit a
null factory, while ASCII forwards to SDL renderer's throwing factory, but all four report
\texttt{true}. Thus ``ask, don't catch'' is the intended API, not yet a universal guarantee.
A caller targeting one of these known exceptions needs the explicit guards above and, for
custom effects, the backend-specific boundary documented in
Chapter~\ref{ch:shader-fx-gap}.

\subsection{The two device-dependent entries, audited end to end}

The remaining two enum values expose why ``inherits \texttt{true}'' and ``has some
implementation code'' are not equivalent to a current-device answer. In the table,
\emph{live} means that \texttt{SupportsCapability()} itself performs a runtime query;
\emph{true} means the backend simply inherits or returns an unconditional answer.

\begingroup
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{longtable}{@{}p{0.16\textwidth}p{0.13\textwidth}p{0.15\textwidth}p{0.48\textwidth}@{}}
\toprule
\textbf{Backend} & \textbf{MSAA report} & \textbf{Anisotropy report} & \textbf{What the live implementation actually establishes} \\
\midrule
\endfirsthead
\toprule
\textbf{Backend} & \textbf{MSAA report} & \textbf{Anisotropy report} & \textbf{What the live implementation actually establishes} \\
\midrule
\endhead
\texttt{SDL\_RENDERER}, \texttt{CANVAS}, \texttt{DX3}
  & false & false & Honest: none of the three implements either feature. \\
\texttt{EASYGL}
  & live & live & Honest live GL checks: maximum samples must exceed one, and
    \texttt{GL\_EXT\_texture\_filter\_anisotropic} must exist. Both paths clamp to the driver. \\
\texttt{VULKAN}
  & true & live & Anisotropy is honestly gated by the enabled device feature. MSAA is real, but
    \texttt{PickSampleCount()} can return one; the capability query never asks it. \\
\texttt{BGFX}
  & true & true & MSAA flags and anisotropic on/off flags are real. The requested anisotropy
    level is ignored, and the default legacy-GL renderer has a pixel-proven MSAA resolve failure
    despite its format-support bit. The query distinguishes neither boundary. \\
\texttt{WEBGPU}
  & true & true & Anisotropic sampler descriptors are real. MSAA performs a scratch 4x texture
    probe and can fall back to one, but the capability query does not consult the probe. \\
\texttt{SDL\_GPU}
  & true & true & Render-target MSAA asks
    \texttt{SDL\_GPUTextureSupportsSampleCount()} and can clamp to one. Anisotropy is stored but
    omitted from commands and sampler creation, so that answer is definitely false. \\
\texttt{D3D9}
  & true & true & Real MSAA and anisotropy calls exist, but both depend on
    \texttt{CheckDeviceMultiSampleType()} / \texttt{D3DCAPS9::MaxAnisotropy}; the capability query
    consults neither, and the anisotropy request is not clamped. \\
\texttt{D3D11}, \texttt{D3D12}
  & true & true & Honest for the devices CNA accepts: both enforce a feature-level~11.0 floor,
    then use the required RGBA8 multisample support and real anisotropic sampler mapping. \\
\texttt{SOFTWARE}
  & true & true & Both false: one CPU sample per pixel; every sampler value is discarded. \\
\texttt{ASCII}
  & true & true & Both false: MSAA forwards to SDL renderer's always-zero path, while sampler
    state is not forwarded at all. \\
\texttt{HEADLESS}
  & true & true & Record-only test semantics: requested values enter traces/state, but no pixel
    is rasterized. These answers cannot establish rendering support. \\
\bottomrule
\end{longtable}
\endgroup

Thus the absolute new false positives are ASCII/Software for MSAA and
ASCII/SDL GPU/Software for anisotropy. Vulkan, WebGPU, SDL GPU, and D3D9 add a subtler MSAA
contract failure: their real application/creation paths can say ``this device has no usable
count above one,'' while the public capability query still says true. D3D9 has the analogous
anisotropy problem. For MSAA, the reliable public evidence after a reset is the
device-clamped value written back into
\texttt{GraphicsDevice.PresentationParameters.MultiSampleCount}, not the capability boolean.
For a \cnaclass{RenderTarget2D} or \cnaclass{RenderTargetCube}, read that target's own
\texttt{MultiSampleCount}; its backend getter reports the applied value (zero means disabled)
on the implementations that genuinely negotiate it.

\subsection{Recovery and debug hooks: one shared flag, two real loss channels, one marker}
\label{sec:backend-recovery-debug-matrix}

Three more interface defaults look like optional backend diagnostics but cross the public
framework boundary in three different ways. First, the device's public
\texttt{SetContextRecovery\allowbreak{}Enabled()} changes its shared recovery flag
\emph{before} it calls the backend hook. The flag
controls whether a successful full \cnaclass{Texture2D} upload may discard its CPU pixel
shadow. When recovery is disabled, a later ordinary \texttt{GetData()} or partial level-zero
update can therefore throw even on a product whose backend hook is empty; a complete
level-zero replacement can still recreate the resource. Unit tests prove all three cases.
Turning recovery back on does not reconstruct already-discarded pixels.

The declaration's instruction to call the setter ``before device initialization'' cannot be
read literally: the public method belongs to an already-constructed
\cnaclass{GraphicsDevice}, whose backend is created eagerly. The intended safe point,
confirmed by the change's own Git history, is after device construction but before loading
game resources. It is a policy for future creation and uploads, not a reversible migration
of existing resources.

Second, non-repeated F9 and F10 key-down events are global \cnaclass{Game} debug triggers.
Its event pump sends the event through \texttt{SdlInputBridge} first, then calls the loss or
restore hook. Game input state thus sees the key as well as the framework action. Third,
\texttt{SetStringMarkerEXT()} is a public device method with a silent interface default.
The full product matrix matters:

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.13}
\begin{longtable}{@{}p{0.13\textwidth}p{0.25\textwidth}p{0.27\textwidth}p{0.27\textwidth}@{}}
\toprule
\textbf{Product} & \textbf{Recovery-policy effect} &
\textbf{F9 / F10 loss channel} & \textbf{String marker and evidence} \\
\midrule
\endfirsthead
\toprule
\textbf{Product} & \textbf{Recovery-policy effect} &
\textbf{F9 / F10 loss channel} & \textbf{String marker and evidence} \\
\midrule
\endhead
\texttt{EASYGL}
  & Shared texture-shadow rule, plus disabling registration of subsequently-created GL
    textures, buffers, queries, SpriteBatch state, and render targets. Existing registrations
    are not removed.
  & Desktop: either key performs one complete synchronous loss/recreate/restore cycle.
    Web: F9 requests asynchronous browser loss; F10 separately requests restore.
  & Marker is a no-op. Desktop recovery is exercised by source/tests; the real browser event
    path was source-audited, not executed here. \\
\texttt{D3D9}
  & The shared flag changes, then the backend hook throws
    \texttt{runtime\_error} through \texttt{NotYetImplemented}; the public call leaves partial
    state.
  & F9 enters \texttt{Lost} and raises \texttt{DeviceLost}; F10 performs a real reset and
    raises \texttt{DeviceResetting}, then \texttt{DeviceReset}. Invalid repeated transitions
    do nothing.
  & Marker also throws. The smoke suite proves event counts, lost-state draw rejection,
    exact post-reset pixels, and default-pool resource recreation. \\
\texttt{VULKAN}
  & Shared texture-shadow rule only; the backend hook is empty.
  & Both keys reach empty hooks.
  & Real, but deferred: a non-empty marker is queued in the pending 3D/current-render-target
    stream, then becomes a Vulkan debug-utils label only when
    \texttt{vkCmdInsert\allowbreak{}DebugUtils\allowbreak{}LabelEXT} is loaded. No dedicated
    marker assertion was found. \\
\texttt{DX3}
  & Shared texture-shadow rule only.
  & Both keys are no-ops, directly confirmed by the backend test.
  & Marker is a no-op. \\
\texttt{SDL\_\allowbreak{}RENDERER}, \texttt{ASCII}, \texttt{BGFX}, \texttt{WEBGPU},
\texttt{SDL\_GPU}, \texttt{D3D11}, \texttt{D3D12}, \texttt{CANVAS},
\texttt{SOFTWARE}, \texttt{HEADLESS}
  & Shared texture-shadow rule only.
  & Both keys reach empty hooks.
  & Marker is a no-op; these rows are source-proven rather than dedicated loss/marker tests. \\
\bottomrule
\end{longtable}
\endgroup

There is no public capability query for any row in this matrix. A portable game should not
bind gameplay to F9/F10 without accounting for the global side effect, should treat
\texttt{SetContextRecoveryEnabled(false)} as a one-way policy for subsequent resource
creation and full uploads, and cannot use successful marker submission as proof that a native
marker was emitted. FNA provides the marker extension by forwarding it uniformly to FNA3D,
but has neither CNA's recovery-policy extension nor these framework-installed F9/F10
triggers.

\section{Build-time selection}

\texttt{cmake/BackendSelection.cmake} is where the backend list from
Chapter~\ref{ch:building-cna} is actually enumerated: the \texttt{CNA\_GRAPHICS\_BACKEND}
cache variable's \texttt{STRINGS} property lists all fourteen recognized values
(\texttt{SDL\_RENDERER}, \texttt{EASYGL}, \texttt{BGFX}, \texttt{VULKAN}, \texttt{WEBGPU},
\texttt{HEADLESS}, \texttt{SOFTWARE}, \texttt{D3D11}, \texttt{D3D12}, \texttt{CANVAS},
\texttt{ASCII}, \texttt{DX3}, \texttt{D3D9}, and \texttt{SDL\_GPU} ---
Chapter~\ref{ch:sdlgpu-backend}'s own dedicated coverage, added later in this book's history
than the other thirteen), and fourteen corresponding boolean
options exist alongside it --- if more than one is somehow set \texttt{ON} at once,
configuration fails outright with ``Exactly one backend option must be ON.'' The default is
itself platform-conditional: \texttt{EASYGL} on Linux or Emscripten, \texttt{SDL\_RENDERER}
everywhere else.

Several backends carry hard, configure-time platform gates rather than soft warnings:
\texttt{D3D9} / \texttt{D3D11} / \texttt{D3D12} fail configuration outright unless
\texttt{CMAKE\_SYSTEM\_NAME} is \texttt{Windows} (native or MinGW-w64 cross-compiled) ---
the comment in the CMake script is blunt about why: ``d3d11.h/d3d12.h/dxgi.h do not exist
elsewhere.'' \texttt{CANVAS} fails configuration outright unless building under Emscripten,
since HTML \texttt{<canvas>} is a browser DOM API with no meaning anywhere else.
\texttt{EASYGL} and \texttt{BGFX} both emit a soft warning (not a hard failure) if configured
outside Linux. \texttt{EASYGL} and \texttt{DX3} both require their respective sibling
checkouts (\texttt{../easy-gl}, \texttt{../free-direct}) to exist at all, hard-erroring
otherwise; \texttt{BGFX} instead fetches \texttt{bkaradzic/bgfx.cmake} directly via CMake's
\texttt{FetchContent}.

\texttt{cmake/BackendLibraries.cmake} wires the resulting per-backend link libraries and
contains two architectural details worth knowing before Chapter~\ref{ch:canvas-ascii-backends}:
the \texttt{ASCII} backend is built by reusing \texttt{SDL\_RENDERER}'s own real \texttt{.cpp}
sources as a separate static library, compiled \emph{without} \texttt{CNA\_BACKEND\_SDL\_RENDERER}
defined so its own \texttt{CreateGraphicsBackend()} factory is compiled out and cannot clash
with ASCII's own factory --- confirming in build-system terms what
Chapter~\ref{ch:canvas-ascii-backends} states in prose: ASCII is composition around
SDL\_Renderer, not a reimplementation. And \texttt{D3D9}'s effect-compilation code
(\texttt{D3D9EffectBackend} / \texttt{D3D9ConstantTable}) is deliberately isolated into its own
static library specifically so that only that isolated library links against
\texttt{d3dcompiler} --- keeping the stock D3D9 rendering pipeline itself free of that
dependency, unlike \texttt{D3D11} / \texttt{D3D12}, which link \texttt{d3dcompiler}
unconditionally into their whole backend target.

\section{One factory function per backend}

Whichever backend is selected at configure time compiles exactly one implementation of
\texttt{std::unique\_ptr\allowbreak{}<IGraphicsBackend>\allowbreak{} CreateGraphicsBackend(const
GraphicsBackendCreateArgs\&)}, gated behind a \texttt{\#ifdef CNA\_BACKEND\_*} matching the
compile definition \texttt{BackendSelection.cmake} set. \cnaclass{GraphicsBackendCreateArgs}
itself carries the window handle, virtual resolution, presentation mode, context-recovery
flag, multisample count, swap interval, and --- as \texttt{NOXNA} additions introduced
alongside the \texttt{D3D9} backend --- back-buffer/depth-stencil format, fullscreen flag,
and graphics profile, each documented as a field ``every existing backend except D3D9 may
ignore'': a direct, structural acknowledgment that \texttt{D3D9} is the one backend that
actually consults these values the way a real device negotiation would.

With the shared contract established, the next nine chapters cover what each backend
actually does with it.
