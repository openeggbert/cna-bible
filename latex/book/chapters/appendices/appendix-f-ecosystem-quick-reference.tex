\chapter{Ecosystem and Platform Quick Reference}
\label{ch:api-quick-reference-vol2}

This companion to Appendix~\ref{ch:api-quick-reference} maps the framework-service and
sharp-runtime surface used throughout Parts~VII--X. It groups stable API roles and puts the
current implementation boundary beside each one. Exact signatures belong to the pinned headers;
platform delivery, ownership, hostile-input behavior and test evidence belong to the cited
chapters.

\section{Input: Keyboard, Mouse, GamePad, and Touch}
\label{sec:api-input}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endhead
\cnaclass{Keyboard}, \cnaclass{KeyboardState} & Static state snapshots, key up/down and pressed-key
  enumeration; marked scancode, modifier and naming helpers. & SDL delivery is real; layout name,
  physical scancode and XNA key identity must not be collapsed. \\
\cnaclass{Mouse}, \cnaclass{MouseState} & Position, buttons, vertical wheel and desktop capture,
  relative/global helpers. & Logical-presentation, window, physical-pixel and post-present
  coordinates diverge by renderer and host. \\
\cnaclass{GamePad}, capabilities and state & Player-indexed state, dead-zone modes, vibration,
  buttons/sticks/triggers; marked identity, light, trigger-rumble and sensor additions. & Device
  open/hotplug, SDL mapping and modern-controller extensions have separate evidence. \\
\cnaclass{TouchPanel}, collection and gesture types & Touch snapshot, display metadata, enabled
  gestures and gesture dequeue, plus platform-injection plumbing. & Mouse/touch substitution is
  not physical-touch proof; Web and mobile coordinate/lifecycle paths differ. \\
\cnans{CNA::Input} services & Clipboard, joystick, haptics, sensors, power and enumerated input
  devices outside XNA's surface. & These are CNA extensions and can have platform-specific
  absence/refusal semantics. \\
\bottomrule
\end{longtable}
\end{small}

Chapter~\ref{ch:input-system} owns the exact SDL event/state paths and coordinate matrix. A state
object being constructible without hardware is not evidence that a host delivered the event or
that the selected renderer maps its coordinates back into the same logical space.

\section{Audio and Media}
\label{sec:api-audio-media}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endhead
\cnaclass{SoundEffect},\newline
  {\scriptsize\texttt{SoundEffectInstance}}\index{SoundEffectInstance} &
  Loaded/raw PCM sound, fire-and-forget or
  owned instance playback, volume/pitch/pan/loop and listener/emitter \texttt{Apply3D}. & The
  3D model is a CNA-side approximation over SDL3\_mixer; source/instance disposal and channel
  ownership are observable contracts. \\
{\scriptsize\texttt{DynamicSoundEffect}}\newline\texttt{Instance}%
  \index{DynamicSoundEffectInstance} &
  Buffer-needed event and streamed PCM submission, plus a
  marked float-buffer path. & Int16 and float modes cannot be mixed silently; queue timing and
  callback thread/order need runtime evidence. \\
XACT objects & Wave banks, sound banks, cues, categories, variables and RPC/DSP-like authored
  behavior. & CNA parses and plays a substantial subset through its own engine; unsupported
  fields and malformed boundaries remain explicit. \\
\cnaclass{Song}, \cnaclass{MediaPlayer} & Song metadata and static queue/playback control,
  shuffle/repeat/mute/volume, position and state-change events. & Ownership, callback order and
  track metadata differ between bare files, library scans and active playback.  The public
  Song/Content extension maps omit \texttt{.mid} even though vendored SDL3\_mixer retains its
  Timidity decoder; decoder presence is not a supported asset route. \\
\cnaclass{Video}, \cnaclass{VideoPlayer} & Video metadata, play/pause/resume/stop, frame texture,
  volume/mute/loop and marked URI/track selection. & FFmpeg availability is platform-conditioned;
  decoded CPU frame, uploaded texture and synchronized playback are distinct milestones. \\
\bottomrule
\end{longtable}
\end{small}

Chapters~\ref{ch:audio-system} and~\ref{ch:media-system} separate decode, object lifetime,
playback and platform codecs. A reader accepting a file is not proof that a mixer/video device
played it or that its events arrived on the intended thread.

\section{Devices and Sensors}
\label{sec:api-devices-sensors}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endhead
Sensor base and readings & Support/state, update interval, current value, start/stop and change
  events for accelerometer, compass, gyroscope and motion-shaped types. & SDL covers only part of
  the sensor vocabulary; Android-native seams, permissions and lifecycle must be named. \\
\cnaclass{VibrateController} & XNA/WP-shaped vibration plus marked intensity/support/device
  helpers. & A stored request is not motor engagement; emulator and physical hardware are
  different evidence tiers. \\
\cnans{CNA::Devices} & Camera, display/system information, dialogs, URL launch, locale,
  clipboard, tray and power services. & Optional \texttt{devices-ext} module and host support;
  many services deliberately fail or return unavailable on headless/unsupported hosts. \\
\bottomrule
\end{longtable}
\end{small}

Chapter~\ref{ch:devices-sensors} documents event queues, thread safety and the gap between
synthetic injection and physical delivery. Chapter~\ref{ch:android-macos} records the
limited Android/macOS artifacts at the pin; it does not promote source presence to mobile
coverage.

\section{Networking and GamerServices}
\label{sec:api-net-gamerservices}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Type group} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endhead
\cnaclass{NetworkSession} & Create/find/join/invited families, local/remote/all/previous gamer
  collections, session state/events, update, host migration and simulated network conditions. &
  Local discovery and ENet delivery are real; they are not Xbox LIVE matchmaking and do not
  supply arbitrary Internet traversal. \\
{\scriptsize\texttt{LocalNetworkGamer}}\index{LocalNetworkGamer},\newline
  \cnaclass{NetworkGamer} &
  Identity/host/local/ready/round-trip state,
  send/receive, voice-like and session-owned relationships. & Packet framing, peer identity,
  ordering and leave/disconnect transitions require two-process hostile-input tests. \\
\cnaclass{SignedInGamer} and related service types & Local identity,
  persistence, paging/rating and selected overlay behavior. & Synthetic/local substitutes preserve
  API shape but do not claim Xbox LIVE service equivalence. \\
\cnaclass{AvatarRenderer} and related types & Faithful base Avatar API plus opt-in
  \cnaclass{SkinnedModelEXT} real-rendering route. & Base existence and extension rendering are
  deliberately separate; neither supplies Microsoft's remote avatar service/assets. \\
\bottomrule
\end{longtable}
\end{small}

Chapters~\ref{ch:networking}, \ref{ch:gamerservices}, and~\ref{ch:avatar} own the delivery,
persistence, hostile-packet, local-substitute, and extension boundaries.

\section{Storage}
\label{sec:api-storage}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endfirsthead
\toprule
\textbf{Type} & \textbf{Public shape} & \textbf{Current boundary} \\
\midrule
\endhead
\cnaclass{StorageDevice} & Fake-async selector/container open, connected/capacity state,
  delete-container, and application-root extensions. & Five focused tests exist; selection is a
  local-filesystem substitute for platform UI. \\
\cnaclass{StorageContainer} & Relative directory/file create/delete/enumerate/existence and
  stream-open operations. & Path containment is absent at the pin and this class has no focused
  test corpus; caller-controlled paths are a security boundary. \\
\bottomrule
\end{longtable}
\end{small}

Chapter~\ref{ch:storage} explains the base-root/container-root asymmetry and fake-async
ownership. Do not summarize the namespace as ``tested'' by transferring StorageDevice evidence
to StorageContainer.

\section{SharpRuntime Core Types Used Everywhere}
\label{sec:api-sharpruntime-core}

\begin{small}
\begin{longtable}{>{\raggedright\arraybackslash}p{3cm}>{\raggedright\arraybackslash}p{5.15cm}>{\raggedright\arraybackslash}p{3.35cm}}
\toprule
\textbf{Type group} & \textbf{Translation role} & \textbf{C\texttt{++} reality} \\
\midrule
\endfirsthead
\toprule
\textbf{Type group} & \textbf{Translation role} & \textbf{C\texttt{++} reality} \\
\midrule
\endhead
\cnaclass{System::Object} and type helpers & .NET-shaped base naming, equality/hash/string and
  selected runtime identity. & Standard C\texttt{++} objects/RTTI; no CLR object header,
  reflection universe, universal boxing or garbage collector. \\
Primitive aliases and \cnaclass{String} & Greppable mapping from C\# primitive vocabulary into
  fixed-width C\texttt{++} types and \texttt{std::string}. & UTF and managed-string semantics
  require explicit per-operation work; an alias is not a CLR. \\
\cnaclass{TimeSpan}, date/time types & Tick-based durations and selected date/time operations. &
  Value types with explicit arithmetic/validation; timezone/runtime breadth is bounded. \\
\cnaclass{EventHandler<T>} & Subscription tokens, add/remove/clear and snapshot-based raise. &
  Combines C\# delegate/event roles explicitly; lifetime and thread safety remain caller/library
  contracts. \\
Collections and task/thread primitives & Source-translation-friendly containers, tasks,
  cancellation and synchronization subsets. & APIs may be implemented, ignored, or intentionally
  out of scope; the database status is not a behavioral parity percentage. \\
\cnaclass{System::IO::Stream}, \cnaclass{MemoryStream}, readers/writers & Abstract byte I/O,
  in-memory storage and .NET-shaped binary/text helpers. & Ownership, closed-state behavior,
  encodings and exception types must be checked per implementation. \\
\bottomrule
\end{longtable}
\end{small}

Chapters~\ref{ch:sharp-object-model}--\ref{ch:sharp-verification-audit} separate API-shaped
translation help from actual C\texttt{++} semantics, include closure and verification. The
sharp-runtime database at the pin contains 16,201 task rows and 2,183 tickets, but those are
workflow classifications with query-defined denominators, not a whole-BCL completion score.

\section{Reading this appendix safely}

Search CNA's module-owned headers for exact framework declarations and sharp-runtime's pinned
headers/database for foundation details. Then require the appropriate source path, runtime,
oracle and host artifact from Appendix~\ref{ch:verification-tiers}. A common type name across
XNA, FNA, CNA and sharp-runtime establishes lineage; it does not by itself establish identical
ownership, exceptions, threading, encoding, platform reach, or behavior.
