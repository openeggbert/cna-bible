\begin{figure}[tbp]
\centering
\begin{figurealt}{CMake receives one public CNA graphics renderer selector. It emits one public renderer identity and compile definition, chooses a factory, and links one implementation family. A family may then call a native API or library. Five selectors map to the single easygl family, while most selectors map one to one.}
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[node distance=9mm and 13mm]
  \node[cna public] (selector) {\texttt{CNA\_GRAPHICS\_RENDERER}};
  \node[cna public, right=of selector] (identity) {public identity\\enum + compile definition};
  \node[cna internal, right=of identity] (factory) {\texttt{CreateGraphicsRenderer()}};
  \node[cna internal, right=of factory] (family) {implementation family};
  \node[cna public, right=of family] (api) {native API / library};
  \node[cna public, below=of identity] (five) {OPENGLES2, OPENGLES3,\\OPENGL33, WEBGL1, WEBGL2};
  \node[cna internal, below=of family] (easy) {one \texttt{easygl} family\\profile chosen at compile time};
  \draw[cna flow] (selector) -- (identity);
  \draw[cna flow] (identity) -- (factory);
  \draw[cna flow] (factory) -- (family);
  \draw[cna flow] (family) -- (api);
  \draw[cna flow] (five) -- (easy);
  \draw[cna flow] (easy) -- (api);
\end{tikzpicture}}
\end{figurealt}
\caption{Identity, factory, family, and native API are different axes. The five-to-one EasyGL
mapping explains the \RendererIdentityCount{}-identity / \RendererFamilyCount{}-family count.}
\label{fig:renderer-identity-family-api}
\end{figure}

