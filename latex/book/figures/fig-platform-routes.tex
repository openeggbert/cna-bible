\begin{figure}[tbp]
\centering
\begin{figurealt}{Five independent inputs compose a CNA application: target OS and toolchain, one platform implementation, a compiled renderer set with one runtime-active renderer, one audio implementation, and the XNA-shaped framework API. Configuration gates validate the combination. Execution and oracle evidence then apply only to that exact combination.}
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}
  \node[cna public, text width=27mm] (target) at (-6.0,2.2) {target OS /\\toolchain};
  \node[cna public, text width=29mm] (platform) at (-3.0,2.2) {\texttt{CNA\_PLATFORM}\\window, event, input};
  \node[cna public, text width=34mm] (graphics) at (0.4,2.2) {renderer set + default\\runtime-active family};
  \node[cna public, text width=34mm] (audio) at (4.1,2.2) {\texttt{CNA\_AUDIO\_PLATFORM}\\playback / capture};
  \node[cna internal, text width=48mm] (compose) at (-1.0,0) {CMake admission + generated registries\\and capability contracts};
  \node[cna public, text width=39mm] (framework) at (4.3,0) {XNA-shaped framework /\\optional CNAEXT or C ABI};
  \node[cna evidence, text width=44mm] (execution) at (-1.0,-2.0) {named artifact, host, platform and\\renderer/service engagement};
  \node[cna evidence, text width=39mm] (oracle) at (4.3,-2.0) {behavior / pixel oracle,\\where retained};
  \draw[cna flow] (target) -- (compose);
  \draw[cna flow] (platform) -- (compose);
  \draw[cna flow] (graphics) -- (compose);
  \draw[cna flow] (audio) -- (compose);
  \draw[cna flow] (compose) -- (framework);
  \draw[cna flow] (compose) -- (execution);
  \draw[cna flow] (framework) -- (oracle);
  \draw[cna flow] (execution) -- (oracle);
\end{tikzpicture}}
\end{figurealt}
\caption{Target, platform, graphics, audio and framework are separate axes. Verification belongs
to the exact composed route; source or configuration admission alone is not execution evidence.}
\label{fig:platform-routes}
\end{figure}
