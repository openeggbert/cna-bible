\begin{figure}[tbp]
\centering
\begin{figurealt}{A verification record combines six coordinates: exact subject and revisions; build and host identity; proof that the intended renderer or service engaged; the observation point; the oracle and tolerance; and the outcome plus retained artifact. Different evidence labels are projections of this record, not rungs on one universal ladder.}
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[node distance=8mm and 12mm]
  \node[cna evidence, minimum width=30mm] (claim) {bounded claim};
  \node[cna internal, above left=of claim] (subject) {subject + revisions};
  \node[cna internal, above right=of claim] (build) {build + host identity};
  \node[cna internal, left=of claim] (engage) {engagement proof};
  \node[cna internal, right=of claim] (observe) {observation point};
  \node[cna internal, below left=of claim] (oracle) {oracle + policy};
  \node[cna internal, below right=of claim] (artifact) {outcome + artifact};
  \draw[cna flow] (subject) -- (claim);
  \draw[cna flow] (build) -- (claim);
  \draw[cna flow] (engage) -- (claim);
  \draw[cna flow] (observe) -- (claim);
  \draw[cna flow] (oracle) -- (claim);
  \draw[cna flow] (artifact) -- (claim);
\end{tikzpicture}}
\end{figurealt}
\caption{The evidence vector behind a durable claim. Source, compile, runtime, renderer,
oracle, and pixel labels summarize different populated coordinates.}
\label{fig:verification-vector}
\end{figure}

